# /refine — Refine phases into relay + sub-ticket board items

Compact hook. yah owns the full procedure; this file is a pointer so
the harness pays a few lines instead of the whole document.

**Fetch the procedure, then follow it:**

- With yah's MCP rail: call the `skill.refine` tool.
- Otherwise (any harness, no MCP): `yah board prompt refine`

Do not edit this file — it is generated from yah's embedded skill
procedures (`crates/yah/party/src/skill_procedures/refine.md`)
and is overwritten on the next `yah init`.
