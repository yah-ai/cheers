<!--
@yah:ticket(R018-T4, "Bearer PASETO response shape + /api/me/sessions list+revoke")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:26:12Z)
@yah:status(review)
@yah:phase(P12)
@yah:parent(R018)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:gotcha("cheers-plan.md P12 still says 'Cookie minting: write yah_session on .yah.dev' — that predates R347. No cookie is written. Update cheers-plan.md P12 when this ticket ships.")
@yah:cleanup("MeAuthState's seven type parameters trip clippy::type_complexity, same as the other route modules in cheers-axum — a `pub type` alias per concrete wiring would silence it, but the warning is universal across the crate (Google/Apple/Passkey/MagicLink states all have it). Hold off until the whole crate refactors together.")
@yah:cleanup("tests/common/mod.rs's MemUserStore + MemSessionDirectory are decoupled: revoke_device on the user store doesn't forget() the directory row, so the integration test explicitly calls directory.forget() after the DELETE. A production product (cheers-sqlx PgSessionDirectory) will source both views from the same table so revoke_device flows through naturally — flagged in the handoff next-steps.")
@yah:next("User sign-off needed: review the cheers-axum me module + integration tests. If approved, archive R018-T4 via board.archive. With all four sub-tickets in review, the parent R018 can also be archived (relay status derives from its children — quest Q005 will then show as closed).")
@yah:next("Follow-on for the cheers-sqlx track: ship a PgSessionDirectory impl. The schema needs `binding` somewhere — either a `last_binding TEXT` column added to refresh_tokens (cheapest), or a separate `devices` table keyed on (user_id, device_id) that the refresh rotation updates. Both let one SQL query satisfy SessionDirectory::list_sessions. cheers-sqlx's PgUserStore::revoke_device already revokes refresh chains for the device — composing that with the directory's view means the row drops from /me/sessions on the next list call without an explicit forget(). Open a bug/feature under a new relay when yah-platform's pg schema is ready to land.")
@yah:next("yah-platform scaffolding (R018's original 'cd yah-platform && cargo test' target) remains an open follow-up. All four sub-tickets shipped self-contained inside cheers-axum so yah-platform when it lands is a pure assembly step: cargo add cheers-axum cheers-sqlx cheers-redis, wire concrete impls, mount cheers_axum::{google,apple,passkey,magic_link,me}::router under /auth and /api. No rework needed inside cheers.")
@yah:handoff("LANDED in cheers-axum as new always-on `me` module (no feature gate). (1) cheers-axum/src/me.rs — MeAuthState<V,Rd,M,R,U,W,D> bundles Arc<EdgeVerifier<V,Rd>> + Arc<SessionAuthority<M,R,U,W>> + Arc<D: SessionDirectory>. router(state) mounts GET /me/sessions + DELETE /me/sessions/{device_id} (axum 0.8 path syntax). Products nest under whatever base they pick (R347 yah.dev uses /api). Per-row response is SessionListEntry { device_id, binding, issued_at, expires_at, is_current } — is_current flips on the device whose id matches the verified Claims::device. SessionDirectory trait lives here (NOT in cheers-server per R018-T1's design call): products implement it over their own data — pg join refresh_tokens with a last_binding column, or a separate devices table. cheers-server's trait surface stays minimal — no new SessionStore.")
@yah:handoff("Bearer extraction: pub fn bearer_from_headers(headers) returns &str (distinguishes MissingBearer/MalformedBearer — accepts case-insensitive 'Bearer'/'bearer' per RFC 7235); pub async fn authenticate(headers, edge, now) -> Claims is the verify helper, mapping cheers_core::Error::{Codec, Revoked} to the single RouteError::Unauthorized variant (no oracle on which failure mode). DELETE handler calls SessionAuthority::revoke_device, and if target==current device additionally revoke_session(current_jti) so the edge kills the in-flight access token immediately rather than waiting out the access TTL.")
@yah:handoff("RouteError gains MissingBearer (401 missing_bearer) + MalformedBearer (401 malformed_bearer) + Unauthorized (401 unauthorized) + UnknownDevice (404 unknown_device). The cheers_core::Error and StoreError From impls were ungated (only need cheers_core, which is unconditional) so the /me module compiles on default-features (no providers) too. lib.rs re-exports MeAuthState / SessionDescriptor / SessionDirectory / SessionListEntry at crate root. session.rs + lib.rs module docs updated to the bearer-only response shape (no cookies on .yah.dev per R347). cheers-plan.md §P12 updated to drop the obsolete `yah_session` cookie line and document the Bearer JSON shape.")
@yah:handoff("TESTS: 3 new unit tests in me.rs covering the bearer parser (canonical / lowercase / missing / wrong-scheme / empty-token). 7 new integration tests in tests/me_basic.rs: list_returns_two_entries_with_is_current (asserts is_current flips on caller's device, binding shapes round-trip {kind:passkey} / {kind:oidc_google}), list_rejects_missing_bearer / wrong_scheme / garbage_token (all 401 with stable error codes), revoke_other_device_returns_204_and_drops_it_from_list (caller's bearer still works after), revoke_current_device_kills_the_in_flight_access_token (next request 401s; jti present in revocation set), revoke_unknown_device_returns_404. tests/common/mod.rs grew MemSessionDirectory + MemUserStore::seed_device + test_edge() / test_verifier() / TEST_HMAC_KEY constants (HmacBlobCodec impls both TokenMinter+TokenVerifier; we build two instances from one key for origin+edge halves). Plus a new doctest example for me.rs.")
@yah:handoff("Verify: cd external/cheers && cargo test -p cheers-axum --all-features → 12 unit + 4 magic-link + 4 google + 1 google round-trip + 3 apple + 3 passkey + 7 me + 4 doctests all green. cargo test --workspace --all-features → 26 test binaries, all green (153+ unit, every integration suite, every doctest). cargo deny --all-features check advisories licenses sources → ok. Full `cargo deny --all-features check` still trips the PRE-EXISTING xlb-net wildcard from cheers/Cargo.toml lines 86+130 (T1+T2+T3 already flagged); not introduced here. No new ignores added to deny.toml.")
@yah:verify("cd external/cheers && cargo test -p cheers-axum --all-features")
@yah:verify("cd external/cheers && cargo test --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check advisories licenses sources")
-->

<!--
@yah:ticket(R018-T3, "Passkey + magic-link routes")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:26:12Z)
@yah:status(review)
@yah:phase(P12)
@yah:parent(R018)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:next("R018-T4 (Bearer PASETO + /api/me/sessions list+revoke) picks up next. SessionBody is already shaped as Bearer across all four route modules now (google, apple, passkey, magic_link). T4 adds an axum extractor that reads `Authorization: Bearer <paseto>` and uses cheers_verify::EdgeVerifier to authenticate the request; mounts GET /api/me/sessions over UserStore::list_devices (joined with refresh_token metadata for binding/expires; per T1's note this is product-side, no new SessionStore trait) and DELETE /api/me/sessions/:device_id over SessionAuthority::revoke_device + revoke_session(jti).")
@yah:next("Decision still deferred to user review: the four route modules return SessionBody JSON (Bearer). Per R018-T4's @yah:gotcha and @yah:next bullets, R347 already landed the no-cookie decision for .yah.dev — cheers-plan.md §P12's `yah_session` Set-Cookie line is now obsolete. T4 should update cheers-plan.md §P12 to drop that line when it ships.")
@yah:handoff("LANDED in cheers-axum as two new modules — `passkey` (feature `passkey`) + `magic_link` (feature `email`). Same reusable-impl-crate pattern as T2; no yah-platform scaffold dependency.")
@yah:handoff("(1) cheers-axum/src/passkey.rs — PasskeyAuthState<M,R,U,W,P,F> bundles Arc<PasskeyRelyingParty> + Arc<SessionAuthority> + Arc<P: PasskeyCredentialStore> + Arc<F: PasskeyFlowStore>. New PasskeyFlowStore trait (with MemoryPasskeyFlowStore impl) stashes PasskeyRegistration/PasskeyAuthentication state server-side between start and finish requests — the same single-use + confidential contract OidcFlowStore carries. The four routes (POST /passkey/{register,authenticate}/{start,finish}) JSON-encode CreationChallengeResponse / RequestChallengeResponse via webauthn-rs's existing serde impls. WebAuthn user handle is derived deterministically from cheers UserId via uuid::Uuid::new_v5(NAMESPACE_OID, ...) so no UserId↔Uuid table is needed. register/finish persists via passkey_to_credential + PasskeyCredentialStore::put. authenticate/finish identifies the answering device by matching cred_id against PasskeyCredentialStore::list_for_user, folds any counter advance back via apply_authentication_result + PasskeyCredentialStore::update, then mints with DeviceBinding::Passkey. Returns SessionBody on both finishes.")
@yah:handoff("(2) cheers-axum/src/magic_link.rs — MagicLinkAuthState<M,R,U,W,S,MA> bundles Arc<MagicLinkProvider<S: UsedJtiStore>> + Arc<MA: Mailer> + Arc<SessionAuthority> + MagicLinkEmail template. Routes: POST /magic-link/request {email} mints a token via MagicLinkProvider::request, renders through the MagicLinkEmail template, hands to the Mailer, returns {ok:true} (token never echoed); GET /magic-link/verify?token=... atomically consumes the token (replay → already_used), find_by_provider+create+link_provider on ProviderKey::Email, mints with DeviceBinding::EmailMagicLink. Per the cheers magic-link contract, the email lives *inside* the signed token claims so an attacker who steals one token can't re-aim it.")
@yah:handoff("(3) error.rs — added RouteError variants Ceremony / UnknownCredential / AlreadyUsed / InvalidEmail / WrongPurpose / MagicLinkToken / MailerBuild / MailerTransport with stable error codes (400/401/500/502). From impls bridge PasskeyError, MagicLinkError, MailerError. Existing StoreError/cheers_core::Error bridges widened to cover the new feature gates. session.rs from_new_session() gate widened to the four features.")
@yah:handoff("(4) Cargo.toml — added two new features mirroring google/apple shape: `passkey` (pulls dep:cheers/passkey, dep:uuid with `v5` feature for the user handle derivation, dep:getrandom, dep:base64); `email` (pulls dep:cheers/email — trait-only, no SMTP transport pulled). Doc-comment examples on both new modules; no new lib unit tests beyond uuid stability + device-id smoke.")
@yah:handoff("(5) TESTS: 2 new integration suites + 1 new test fixture. (a) tests/passkey_basic.rs — full register→authenticate round-trip driven by webauthn-authenticator-rs's SoftPasskey (added as dev-dep, mirrors cheers/src/passkey/'s own tests): start_registration returns flow_id+challenge → SoftPasskey signs → finish_registration persists Credential + mints session → start_authentication uses list_for_user → SoftPasskey assertion → finish_authentication folds any counter advance and mints a fresh session with a different jti; plus negative tests (unknown flow_id rejected, empty credential list rejected). (b) tests/magic_link_basic.rs — request→capture-via-CapturingMailer→verify round-trip including replay rejection, plus invalid-email and malformed-token cases, plus the idempotent-relogin assertion that ProviderKey::Email + find_by_provider returns the same user on repeat. (c) tests/common/mod.rs — added MemPasskeyStore (real Mutex<HashMap<(UserId, DeviceId), Credential>>) alongside the existing StubPasskeyStore for OIDC tests that don't need it. Gated the openidconnect/wiremock helpers behind #[cfg(any(feature='google', feature='apple'))] so the passkey/email-only build doesn't drag them in.")
@yah:handoff("Verify: cd external/cheers && cargo test -p cheers-axum --features passkey,email → 10 unit + 4 magic-link + 3 passkey + 2 doctests all green. cargo test --workspace --all-features → all crates green (153+ unit, every integration suite passing, doctests included). cargo deny --all-features check advisories licenses sources → ok. Full `cargo deny --all-features check` still trips the PRE-EXISTING xlb-net wildcard from cheers/Cargo.toml lines 86+130 (T1+T2 already flagged); not introduced here. No new ignores added to deny.toml.")
@yah:verify("cd external/cheers && cargo test -p cheers-axum --features passkey,email")
@yah:verify("cd external/cheers && cargo test --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check advisories licenses sources")
@yah:gotcha("T3 deviated from the literal yah-platform-test verify command for the same reason T1/T2 did — yah-platform still does not exist. cheers-axum is the reusable-impl crate yah-platform will `cargo add` + mount.")
@yah:gotcha("Passkey routes use a flow_id-in-body design rather than cookies. Different from OIDC's CSRF cookie binding because passkey ceremonies never leave the relying party's origin: the WebAuthn challenge already binds the assertion to origin, so a separate CSRF cookie adds nothing. Body-field also dodges the cross-site cookie pain SPAs hit.")
@yah:gotcha("WebAuthn user handle derivation lives in passkey.rs as uuid_from_user_id() — uuid::Uuid::new_v5(NAMESPACE_OID, user_id.bytes()) → cheers::passkey::Uuid via from_bytes. Stable per user, no extra table. webauthn-rs re-exports Uuid but its transitive uuid dep doesn't enable v5; we name uuid as a direct dep (gated on `passkey` feature) for the constructor.")
@yah:gotcha("cargo deny --all-features check bans still trips on the PRE-EXISTING xlb-net wildcard (T1+T2 already flagged). Verify commands narrow to advisories/licenses/sources which pass clean. No new ignores were added in T3.")
@yah:assumes("yah-platform when it lands will compose cheers_axum::{passkey,magic_link} routers directly, the same way T2 assumed for google/apple. Crate stays product-agnostic — no domain hardcoding, no DATABASE_URL, no yubaba coupling.")
@yah:assumes("Bearer JSON response shape is the right choice across all four route modules. If a product wants cookies, it wraps SessionBody in middleware; or T4 can ship a CookieMode toggle. Decision still pending user sign-off (carries over from T2).")
@yah:cleanup("MagicLinkAuthState exposes no rate-limiting; a malicious caller can flood the mailer with garbage emails (one request → one send) — a product that exposes this to untrusted callers must layer a rate-limiter (axum tower::limit, governor) in front. Flagged here so the layering doesn't get forgotten.")
@yah:cleanup("PasskeyFlowStore has only a MemoryPasskeyFlowStore impl in cheers-axum. A redis-backed impl mirroring cheers-redis::RedisRefreshStore would let multi-replica deployments share registration/authentication state across instances — natural follow-up in cheers-redis when a product actually needs HA passkey.")
-->

<!--
@yah:ticket(R018-T2, "OIDC routes — /auth/login/{google,apple} + /auth/callback/{google,apple}")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:26:12Z)
@yah:status(review)
@yah:phase(P12)
@yah:parent(R018)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("LANDED in new crate `cheers-axum` (mirrors T1's reusable-impl-crate pattern; user-approved via mcp__yah__ask_user). Workspace member added to Cargo.toml. axum 0.8 routes + the SessionAuthority/UserStore wiring R018-T1's handoff described, with no yah-platform scaffold dependency.")
@yah:handoff("(1) cheers-axum/src/lib.rs — module index + re-exports (CsrfCookieConfig, RouteError, SessionBody). Feature gates: `google` and `apple`, each pulling `cheers` with the matching provider feature + reqwest + openidconnect + getrandom + base64. (2) cookie.rs — CsrfCookieConfig (HttpOnly + SameSite=Lax default; `for_apple()` ctor flips to SameSite=None for the cross-site form-post callback). set_cookie/clear_cookie helpers and read_cookie() parser; tiny on purpose — no `cookie` crate dep. 6 unit tests. (3) error.rs — RouteError with stable {error,message} JSON + status_and_code() mapping. From impls bridge StoreError/cheers_core::Error/OidcError/AppleRedirectError. (4) session.rs — SessionBody JSON shape {access_token, token_type='Bearer', access_expires_at, refresh_token, refresh_expires_at, user_id, device_id, jti}. R018-T4 will refine the bearer/PASETO surface, but this is the superset T4 builds on. (5) google.rs — GoogleAuthState<S,M,R,U,W>, GET /login/google (302 + Set-Cookie), GET /callback/google?code+state (verifies CSRF cookie matches state, finishes OIDC, find_by_provider or create+link_provider, SessionAuthority.establish with DeviceBinding::OidcGoogle, clears the cookie, returns SessionBody JSON). Per-login DeviceId is 16 random bytes base64url (same shape as the SessionAuthority jti). router(state) helper composes the two routes. (6) apple.rs — AppleAuthState, GET /login/apple (302 + SameSite=None cookie + response_mode=form_post baked into authorize_url by AppleRedirectProvider), POST /callback/apple (raw String body so AppleCallbackForm::parse keeps the `user` JSON intact; routes FirstLoginName into NewUser::with_name on first auth).")
@yah:handoff("TESTS: 8 lib unit tests + 8 integration tests pass. (a) tests/google_basic.rs — login_redirects_to_idp_and_sets_csrf_cookie / callback_without_cookie_is_400_missing_csrf / callback_with_mismatched_cookie_is_400_state_mismatch / callback_with_provider_error_param_is_400. (b) tests/google_round_trip.rs — full /login → wiremock /token (with id_token bound to the stashed nonce) → /callback round-trip; asserts SessionBody shape + UserStore u-1 created with email+name from id_token. (c) tests/apple_basic.rs — login_redirects_to_apple_with_samesite_none_cookie / callback_without_cookie_is_400_missing_csrf / callback_with_apple_error_body_is_400_provider. (d) tests/common/mod.rs — MemUserStore (lookup_email helper) + MemRefreshStore + MemRevocations (impl RevocationReader+Writer for the test rig) + StubPasskeyStore + signing_key()+mount_discovery_and_jwks() wiremock fixtures + a HmacBlobCodec test minter. p256 added to dev-deps for the deterministic Apple .p8 key.")
@yah:handoff("Verify: `cd external/cheers && cargo test --workspace --all-features` → all green (153+ unit tests, 8 cheers-axum integration tests, doctests including the GoogleAuthState wiring example). `cargo deny --all-features check advisories licenses sources` → ok. Full `cargo deny --all-features check` still trips on the PRE-EXISTING xlb-net wildcard from cheers/Cargo.toml lines 86+130 (T1 already flagged this; not introduced here). T2 added no new ignores to deny.toml.")
@yah:next("R018-T3 (Passkey + magic-link routes) picks up next. The same pattern: add `cheers_axum::passkey` and `cheers_axum::magic_link` modules behind `passkey`/`email` features. PasskeyAuthState bundles a `PasskeyRelyingParty` + a `PasskeyCredentialStore` (the trait from R018-T1's cheers-server addition) + the SessionAuthority; routes are `/auth/passkey/register/{start,finish}` + `/auth/passkey/authenticate/{start,finish}`. Authentication's start_authentication call needs allow_credentials = list_for_user(user_id) — wire that as a lookup. After finish_authentication returns PasskeyUpdate::Updated, call PasskeyCredentialStore::update to persist the counter. MagicLinkAuthState wires MagicLinkProvider (R010-T1) + a Mailer + UsedJtiStore — routes are POST /auth/magic-link/request (mints token, mails URL) + GET /auth/magic-link/verify?token=... (verifies, lookups/creates user keyed on Email provider, establishes session). Reuse cookie.rs + error.rs + session.rs verbatim.")
@yah:next("R018-T4 (Bearer PASETO + /api/me/sessions list+revoke) picks up after T3. SessionBody is already shaped as Bearer; T4 mostly adds an axum extractor that pulls `Authorization: Bearer <paseto>` and uses cheers_verify::EdgeVerifier to authenticate the request. /api/me/sessions list endpoint joins UserStore::list_devices with the live refresh_token rows (per T1's note: in product code, no new SessionStore trait). /api/me/sessions/:device_id DELETE calls SessionAuthority::revoke_device + revoke_session on the active jti.")
@yah:next("Decision deferred to user review: the OIDC routes return JSON SessionBody (Bearer pattern) rather than setting a `yah_session` cookie on `.yah.dev` (per cheers-plan.md §P12). The Bearer shape is more reusable across product surfaces (noisetable iOS app vs. yah.dev web vs. desktop). A consumer that wants the cookie pattern can wrap SessionBody in middleware; alternatively T4 could ship a `CookieMode` toggle on the auth states. Awaiting sign-off on which path before T4 lands.")
@yah:verify("cd external/cheers && cargo test -p cheers-axum --features google,apple")
@yah:verify("cd external/cheers && cargo test --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check advisories licenses sources")
@yah:cleanup("openidconnect's `url` crate is re-exported through `openidconnect::url` — `tests/google_round_trip.rs` uses that path rather than a direct dep. If we ever need URL parsing in non-OIDC code paths, add `url` as a direct dep.")
@yah:cleanup("The doc-comment example in google.rs uses `Box<dyn std::error::Error>` rather than anyhow to keep the doctest crate-graph minimal. apple.rs has no doc example yet (could mirror google.rs's pattern in T3/T4).")
@yah:gotcha("T2 deviated from the literal verify command (`cd yah-platform && cargo test`) because yah-platform still does not exist — same situation T1 flagged. User pre-approved a new `cheers-axum` crate via mcp__yah__ask_user (matches T1's reusable-impl-crate pattern). When yah-platform eventually scaffolds, it just `cargo add cheers-axum` + composes routes; no rework needed.")
@yah:gotcha("axum 0.8 is the chosen version (hyper 1 / http 1, dedupes vs reqwest). axum 0.7 would also have worked but 0.8 is current.")
@yah:gotcha("cargo deny --all-features check bans still fails on the PRE-EXISTING xlb-net wildcard dep in cheers/Cargo.toml (T1 already documented). Verify commands above narrow to advisories/licenses/sources which pass clean.")
@yah:assumes("yah-platform when it lands will compose `cheers-axum::{google,apple}` route handlers directly rather than rewriting them. The crate is product-agnostic — no cookie-domain hardcoding, no DATABASE_URL, no yubaba coupling.")
@yah:assumes("SessionBody (Bearer JSON) is the right response shape for T2. If the user prefers the cookie shape from cheers-plan.md §P12 (`yah_session` on `.yah.dev`), T4 needs to add a `CookieMode` toggle or middleware layer — flagged as the deferred decision in next() above.")
-->

<!--
@yah:ticket(R018-T1, "pg-backed UserStore impl (users, oauth_identities, refresh_tokens, passkey_credentials)")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:26:12Z)
@yah:status(review)
@yah:phase(P12)
@yah:parent(R018)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("LANDED across cheers-server + two new crates. (1) cheers-server: added PasskeyCredentialStore trait alongside UserStore/RefreshStore — async put/list_for_user/delete/update over cheers-core's Credential type (no webauthn-rs dep in the trait surface; products bridge via cheers::passkey::passkey_to_credential / passkey_from_credential). Added RefreshTokenRecord::new() constructor so external store impls can construct one despite #[non_exhaustive]. 35 unit + 9 proptest + 2 doctest pass. (2) NEW crate cheers-sqlx — sqlx-backed impls for UserStore, RefreshStore, PasskeyCredentialStore, RevocationWriter+Reader. Features ['pg','sqlite','passkey','pg-integration']. Schema in migrations/{pg,sqlite}/0001_initial.sql — four tables: users (PK user_id, partial-unique email), oauth_identities (composite PK provider+issuer+subject; issuer is '' for non-generic providers so the PK holds), refresh_tokens (PK token, indexed on chain_id + (user_id, device_id) + expires_at), passkey_credentials (PK (user_id, device_id), material as JSONB on pg / TEXT on sqlite), revocations (PK jti, indexed on expires_at for cron GC). sqlx 0.8 with runtime-tokio-rustls (no native-tls, deny.toml stays clean). PG_MIGRATIONS / SQLITE_MIGRATIONS Migrator statics for startup. Both backends run the same contract scenarios via tests/common/mod.rs — 6 sqlite tests (always-on, sqlite::memory:) + 6 pg tests (feature pg-integration, testcontainers postgres) all green. UserStore::list_devices derives the active-devices view from refresh_tokens (DISTINCT device_id WHERE NOT revoked), revoke_device flips refresh chains for (user, device) — keeps the trait single-trait per the user's design call (no separate SessionStore). (3) NEW crate cheers-redis — RedisRefreshStore + RedisRevocationStore (impls both RevocationWriter and RevocationReader). Feature redis-integration for tests. redis 0.27 with tokio-rustls-comp + connection-manager. Key layout: cheers:refresh:{token} (JSON, EX = expires_at-now), cheers:chain:{chain_id} (SET of tokens for revoke_chain without SCAN), cheers:revoked:{jti} (string, EX = access_ttl). RedisRevocationStore::with_revoke_ttl_seconds tunes TTL (defaults to SessionPolicy::DEFAULT_ACCESS_TTL_SECONDS = 15min). Per-crate with_prefix() for multi-tenant redis isolation — verified by refresh_store_prefix_isolation test. 5 redis-integration tests (testcontainers redis) all green including a real-time TTL expiry test (sleeps 1.2s past a 1s TTL, asserts is_revoked flips). (4) deny.toml: added advisory ignores for tokio-tar (RUSTSEC-2025-0111 — testcontainers dev-dep only, never shipped), rustls-pemfile (RUSTSEC-2025-0134 — unmaintained, not a CVE, reaches us via testcontainers + redis + rustls-native-certs), paste (RUSTSEC-2024-0436 — pre-existing via xlb-net netlink path, unmaintained not vulnerable). cargo deny --all-features check: advisories/licenses/sources ok. (Bans still surfaces a PRE-EXISTING wildcard on the cheers crate for xlb-net path deps lines 86/130 — not introduced by this ticket.)")
@yah:next("R018-T2 picks up next (OIDC routes — /auth/login/{google,apple} + /auth/callback/{google,apple}). The route handlers consume what landed here: yah-platform's auth module (path TBD when yah-platform exists) wires PgUserStore + PgPasskeyCredentialStore (identity tier) + RedisRefreshStore + RedisRevocationStore (TTL tier), assembles a cheers_server::SessionAuthority<PasetoV4SecretMinter, RedisRefreshStore, PgUserStore, RedisRevocationStore>, and exposes the OIDC routes calling provider.begin() at login + provider.finish() at callback, mapping the verified id_token into the user store via find_by_provider → create + link_provider. The hybrid wiring (pg for users + redis for sessions) is what the design call landed on — single-engine deployments can substitute PgRefreshStore + PgRevocationStore for the redis pair and skip the redis dep entirely.")
@yah:next("R018-T3 (Passkey + magic-link routes) consumes PgPasskeyCredentialStore via PasskeyCredentialStore::{list_for_user, put, update, delete} — list_for_user → allow_credentials for start_authentication; put after finish_registration; update after apply_authentication_result returns PasskeyUpdate::Updated. Magic-link side uses MagicLinkProvider (R010-T1) with a UsedJtiStore; the trait is async so a redis-backed impl can land in cheers-redis later (R010-T1's MemoryUsedJtiStore note flagged this).")
@yah:next("R018-T4 (Bearer PASETO + /api/me/sessions list+revoke) wants per the existing ticket annotation a list endpoint over device_id+binding+issued_at+expires_at+is_current. Per the design discussion: the join lives in product code, NOT a new SessionStore trait. yah-platform's handler queries UserStore::list_devices(user_id) → for each device, look up the live refresh_token row's metadata. With redis backing the refresh store this is N round-trips (acceptable for a /me page); with pg backing it's a single JOIN. Implementer can wrap it in a small helper if reused across products.")
@yah:verify("cd external/cheers && cargo test --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check advisories licenses sources")
@yah:gotcha("PRE-EXISTING (not introduced by T1): cargo deny --all-features check bans fails with 'wildcard dependencies for crate cheers' on cheers/Cargo.toml lines 86 + 130 (xlb-net = { path = ..., optional = true } without a version field). Verify commands above run deny on advisories/licenses/sources only, skipping bans. Fold the wildcard fix into a future workspace-hygiene pass — adding version='*' on those path deps would silence it (or pinning to a real version once xlb-net cuts releases).")
@yah:gotcha("cheers-sqlx's SqliteRefreshStore stores BOOLEAN as 0/1 INTEGER (sqlite has no native bool); the Decode path reads i64 and compares != 0. cheers-server's RefreshTokenRecord::consumed/revoked are bool — round-trip verified by tests, but a Migrator that drops the existing schema then re-creates with a different type would have to keep this convention.")
@yah:gotcha("Redis impls hold an ARC<ConnectionManager> internally via Clone; passing the same fixture to multiple stores (refresh + revocation) shares the underlying connection pool. The redis crate's ConnectionManager auto-reconnects on connection loss, which is the only production-safe primitive — bare MultiplexedConnection drops in-flight requests on network blips.")
@yah:assumes("yah-platform workspace still does not exist on disk. R018-T1's original verify (cd yah-platform && cargo test) was unrunnable — the reusable impl crates approved on this turn make T1 self-contained inside cheers's workspace; the yah-platform wiring step belongs to R018-T2/T3/T4 (or to a yet-to-be-created yah-platform scaffold relay).")
-->

<!--
@yah:ticket(R017-T2, "ConfirmationStrategy trait + SixDigitCode/DisplayCode/AutoTrust impls")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:26:12Z)
@yah:status(review)
@yah:phase(P10)
@yah:parent(R017)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("Implemented crates/cheers/src/lan_pair/confirm.rs — ConfirmationStrategy async trait (async_trait), AutoTrust (always accept, no user interaction), SixDigitCode (user enters code from offerer console; code in PairOffer::code over secure QUIC channel, trimmed string equality), DisplayCode (callback shows code to user, returns bool). @yah:assumes: default UX is six-digit code on first-boot console per design doc §Open decisions #4; display-detection auto-switch not yet decided.")
@yah:verify("cargo test -p cheers --features lan-pair")
@yah:assumes("Default confirmation UX is six-digit code on rpi first-boot console (design doc §'Open decisions' #4). Display-detection auto-switch alternative is not yet decided — confirm with user before implementing.")
-->

<!--
@yah:ticket(R017-T1, "lan_pair offerer + accepter over xlb-net Endpoint")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:26:12Z)
@yah:status(review)
@yah:phase(P10)
@yah:parent(R017)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("Implemented crates/cheers/src/lan_pair/ — mod.rs (PairOffer, PairAccept, LanPairError, AccepterMsg wire types, ALPN const), offerer.rs (Offerer::wait_for_pair over raw endpoint.accept()), accepter.rs (Accepter::pair with generic ConfirmationStrategy). Protocol: offerer opens bidi QUIC stream, sends PairOffer JSON, reads AccepterMsg response. 6 integration tests (AutoTrust/SixDigitCode/DisplayCode paths, code mismatch rejection, random_code shape). All 125 cheers tests green.")
@yah:verify("cargo test -p cheers --features lan-pair")
-->

<!--
@yah:ticket(R015-T3, "store/memory.rs — MemoryStore for tests")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:26:12Z)
@yah:status(review)
@yah:phase(P8)
@yah:parent(R015)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("Landed cheers/src/store/mod.rs + cheers/src/store/memory.rs. MemoryStore is Arc<RwLock<HashMap>> under the headless feature flag. Clone shares the backing map. 5 tests: round-trip, overwrite, multi-key independence, clone-shares-state, dyn-compatible. All 124+9 tests green for both --features headless and --features keyring. store module is gated #[cfg(any(feature=headless, feature=keyring))] in lib.rs, extensible for the KeyringStore and EncryptedFileStore follow-ons.")
-->

<!--
@yah:ticket(R015-T2, "store/encrypted_file.rs — EncryptedFileStore via age (TPM key if present)")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:26:12Z)
@yah:status(review)
@yah:phase(P8)
@yah:parent(R015)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("LANDED, ready for sign-off. EncryptedFileStore at crates/cheers-store/src/encrypted_file.rs behind the `headless` feature (= dep:age + async-trait + serde_json); exported from lib.rs alongside the free fn tpm_device_present(). impls cheers_core::CredentialStore over an age-encrypted file. Storage model: the WHOLE keyed map (BTreeMap<String,Credential>) is serde_json-encoded then age-encrypted to one file; every put/delete is a read-decrypt-modify-encrypt-write cycle serialized behind a Mutex<()>, written to a sibling `.age.tmp` then renamed over the target (crash-safe). open(data_path, key_path) acquires an age::x25519 identity: loads key_path if present, else generates + writes it create_new with mode 0600 on unix (re-reads on AlreadyExists to tolerate a first-run race). delete of an absent key => StoreError::NotFound; all I/O + age + JSON failures => StoreError::Backend(msg), including a wrong-key/tampered-file decrypt (authenticated cipher refuses). Custom Debug redacts the identity (age::x25519::Identity has no Debug + it's secret). Tests (11 unit + 1 doctest, network-free via tempfile): round-trip, missing=>None, delete-absent=>NotFound, overwrite, distinct keys, persists-across-reopen, data-file-encrypted-at-rest (asserts cleartext material absent + age header present), wrong-key=>Backend, key-file-is-0600 (unix), Send+Sync+dyn, tpm-absent-on-dev. GREEN: cargo test -p cheers-store --features keyring,headless (19 unit +1 ignored real-backend +2 doctest); clippy --all-targets clean; cargo check/test --workspace --all-features; cargo deny --all-features (advisories/bans/licenses/sources ok — age + transitive deps all permissive, NO new exceptions). cargo tree confirms NO openssl/native-tls and NO token crypto (pasetors/openidconnect/webauthn/argon2) — age is at-rest ChaCha20-Poly1305/RustCrypto, the right kind for a device credential store.")
@yah:next("R015-T3 (store/memory.rs — MemoryStore for tests) is the last P8 sub-ticket. It lands in cheers-store too (HOME DECISION on R015-T1). Per the plan it's the always-on test store — a process-local Mutex<HashMap<String,Credential>>, same shape as the MemCredentialStore already in cheers-core/src/store.rs::tests; either always-compiled (no feature gate) or test-only, NOT behind a feature flag. Once R015-T1+T2+T3 are signed off, archive each child then board.archive R015; update R015's parent verify from `-p cheers` to `-p cheers-store --features keyring,headless` when closing (the device impls moved crates post-F5).")
@yah:assumes("TPM-sealed keys are DEFERRED, not built: the plan wants the age key sealed to a TPM when /dev/tpm0 exists, but real sealing needs tss-esapi (heavyweight, Linux-only, untestable on the macOS/CI dev boxes). Shipped the file-key path + a tpm_device_present() probe so a caller can warn on fallback; open() is the file-key seam and a future open_tpm(data_path) would acquire the same age::x25519::Identity from a sealed blob with the rest of the store unchanged. Flag if a real TPM-equipped rpi target lands (P10/P9).")
@yah:assumes("Sync file I/O runs inside the async CredentialStore methods (no spawn_blocking — avoids forcing a tokio dep, same choice as KeyringStore). Fine for the device tier's small, infrequent credential writes.")
@yah:cleanup("Whole-map rewrite per mutation is O(map size) per put/delete — correct and simple for a handful of device credentials, but if a host ever holds many, switch to per-key age files or a real on-disk keyed store. Not worth it until a symptom shows.")
@yah:verify("cd external/cheers && cargo test -p cheers-store --features keyring,headless")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
-->

<!--
@yah:ticket(R015-T1, "store/keyring.rs — KeyringStore over keyring crate")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:26:12Z)
@yah:status(review)
@yah:phase(P8)
@yah:parent(R015)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:depends_on(R019-F6)
@yah:handoff("LANDED but NOT signed off — blocked on R019-F6's workspace, not on R015-T1. New crate crates/cheers-store (Cargo.toml + src/lib.rs + src/keyring.rs + README.md), added to root Cargo.toml members (F6 reconciled the list; cheers-store survived alongside cheers-verify/cheers-server). KeyringStore impls cheers_core::CredentialStore over keyring v3. `cargo test -p cheers-store --features keyring` is GREEN (8 unit + 1 doctest pass, 1 ignored real-backend) — self-contained, depends only on cheers-core's no-crypto surface, so F6's churn in cheers/cheers-core does not affect it. CANNOT run the relay-level verify yet: R019-F6 is mid-refactor and `cargo check --workspace` fails to compile cheers/cheers-core (CodecError: From<pasetors::errors::Error>) — NOT caused by this ticket. The 34 `unexpected cfg value: server` warnings in cheers-core are also F6 transitional; expect them gone once F6 lands.")
@yah:next("RESUME when R019-F6 lands (workspace compiles): (1) confirm cheers-store -> cheers-core dep matches final topology — cheers-core stays the keyless types/traits home, so cheers-store depends on it; if cheers-core ends up with no [features], drop the now-moot default-features=false on the cheers-core dep in crates/cheers-store/Cargo.toml (harmless if left). (2) run the @yah:verify cmds below + `cargo tree -p cheers-store --features keyring` to confirm NO pasetors/openidconnect/webauthn-rs/argon2 (crypto-free device surface is the whole point of the separate crate). (3) if green, board.move R015-T1 to review and ping the user.")
@yah:next("HOME DECISION (user-approved 2026-05-27): device-tier CredentialStore impls live in the NEW crate cheers-store, NOT crates/cheers/src/store/ (the pre-F5 plan anchor) — cheers-store depends on cheers-core's no-crypto surface so a device target never compiles a codec. R015-T2 (encrypted_file/age) + R015-T3 (memory) land in cheers-store too, behind a `headless` feature and (memory) test-only/always-on. Parent R015's verify still says `-p cheers` — update to `-p cheers-store` when R015 closes.")
@yah:next("KEY DECISIONS embodied in the code (don't relitigate): keyring v3 (3.6.3) not v4 (v4 = keyring-core + per-platform store crates + clap/rpassword CLI umbrella, wrong for a lib; cleanup: migrate to keyring-core when it settles — one-file swap behind CredentialStore). Per-target backends apple-native / windows-native / sync-secret-service+crypto-rust, default-features=false, openssl-free. KeyringStore caches one Arc<Entry> per key (REQUIRED — keyring mock persistence is EntryOnly, so rebuilding Entry per call can't round-trip; also a real-backend nicety). serde_json blob via set_secret/get_secret. map_keyring_err: NoEntry handled per-call-site (get=>None, delete=>NotFound); all else incl. NoStorageAccess/PlatformFailure => StoreError::Backend(msg). Tests deterministic via keyring::mock; the #[ignore]'d real_backend_round_trip skips on Backend (headless gotcha) and must run in isolation (mock tests set the builder process-wide).")
@yah:assumes("keyring calls are blocking but run inside the async CredentialStore methods (no spawn_blocking — that would force a tokio runtime dep, which cheers-store avoids). Fine for local, fast credential reads; matches the other store impls' sync-under-async shape.")
@yah:assumes("Backend-unavailable (locked Keychain, no Secret Service on headless Linux) collapses to StoreError::Backend(keyring-message); cheers-core's StoreError has no typed Unavailable variant and R015-T1 doesn't change that shared contract. A typed Unavailable (so a caller can fall back to the encrypted-file store rather than treat it as a bug) would be a cheers-core change — flag to the contract owner if the device tier wants it.")
@yah:verify("cd external/cheers && cargo test -p cheers-store --features keyring")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
-->

<!--
@yah:ticket(R014-T2, "passkey/authenticate.rs — start_authentication → finish_authentication")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:21:45Z)
@yah:status(review)
@yah:phase(P7)
@yah:parent(R014)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("Passkey authentication ceremony landed at crates/cheers/src/passkey/authenticate.rs (mod wired in passkey/mod.rs under #[cfg(feature='passkey')]), mirroring R014-T1's register.rs. impl PasskeyRelyingParty { start_authentication(allow_credentials: &[Passkey]) -> (RequestChallengeResponse, PasskeyAuthentication) [thin wrap over webauthn-rs start_passkey_authentication, errors map to PasskeyError::Ceremony]; finish_authentication(&PublicKeyCredential, &PasskeyAuthentication) -> AuthenticationResult }. allow_credentials is the candidate set (recover via passkey_from_credential) — the non-discoverable profile names the credentials, an empty slice yields an unanswerable challenge by design. finish does NOT mutate the stored credential; per the WebAuthn spec the RP must fold the result back. Surfaced that as a free fn apply_authentication_result(passkeys: &mut [Passkey], &AuthenticationResult) -> PasskeyUpdate<'a> (the next-note's 'surface as a helper' choice): finds the entry whose cred_id matches result.cred_id(), applies Passkey::update_credential, returns PasskeyUpdate #[non_exhaustive] { Updated(&Passkey) [counter/backup-flags advanced — re-persist via passkey_to_credential], Unchanged(&Passkey) [matched but no change — the common synced-passkey case, no write], UnknownCredential [no stored passkey matched — assertion signed by a credential the user doesn't own, REJECT the login] }. Re-exports added to passkey/mod.rs prelude block: AuthenticationResult, PasskeyAuthentication (from webauthn_rs::prelude); apply_authentication_result + PasskeyUpdate (from the new module). Module + 'Persisting the result' docs updated to cover the auth side and drop the 'P7 R014-T2 pending' phrasing. Tests (5 new unit + 1 new doctest, network-free, all via the SoftPasskey dev-dep): start_authentication challenge wire-shape (rpId, challenge non-empty, userVerification=required, allowCredentials lists the stored cred_id); full SoftPasskey register→authenticate round-trip where result.cred_id() == the registered passkey (ONE SoftPasskey instance does both register + auth — it keys the private key by rp_id+cred_id, so register_with(rp, &mut authenticator) shares the instance); finish rejects an assertion bound to a different challenge (state binds the challenge, same shape as register.rs's negative test); apply_authentication_result returns Updated|Unchanged for the answering passkey and identifies it; apply_authentication_result returns UnknownCredential against an empty stored set. cargo test -p cheers --features passkey: 133 unit + 11 doctests pass (was 128 + 10). cargo test --workspace --all-features, cargo check --workspace --all-features, cargo deny --all-features check: all clean. authenticate.rs is clippy-clean (the 2 PRE-EXISTING warnings in email/template.rs (P3) + providers/apple/redirect.rs (P6) carried over from R014-T1's cleanup note remain, unrelated to P7).")
@yah:next("R014-T2 ready for human sign-off. This completes P7's server-side WebAuthn: R014-T1 (register) + R014-T2 (authenticate) are both feature-complete pending sign-off. Once BOTH T-subs are signed off, archive each child (R014-T1, R014-T2) then board.archive R014 to close P7 — relay status derives from children, so don't move R014 itself. P7 is server-side only; the per-platform client UI (browser WebAuthn JS / native AuthenticationServices) is P9, and pg-backed passkey_credentials persistence + the passkey HTTP routes are P12 (R018-T1 / R018-T3), which consume passkey_to_credential / passkey_from_credential + apply_authentication_result from here.")
@yah:assumes("True signature-counter REGRESSION (clone) detection is NOT implemented and cannot be at the cheers layer with webauthn-rs 0.5's public API: Passkey encapsulates the stored counter and exposes only update_credential, which raises the counter monotonically and silently refuses to lower it — so a clone replaying a stale counter surfaces as PasskeyUpdate::Unchanged, indistinguishable from a synced passkey that legitimately never advances. Detecting the regression (flagging vs ignoring) needs the raw asserted-vs-stored comparison, which would require webauthn-rs's danger-credential-internals feature. Documented on apply_authentication_result. Largely moot for the synced-passkey profile cheers ships (most credentials never advance a counter). Revisit if a deployment enrolls counter-bearing authenticators (e.g. hardware security keys) and wants active clone alarms.")
@yah:assumes("apply_authentication_result lives as a free fn taking &mut [Passkey] (parallel to register.rs's passkey_to_credential / passkey_from_credential free fns) rather than a method on PasskeyRelyingParty, because the update is pure credential bookkeeping with no relying-party state — it needs the user's stored passkeys + the result, nothing from the RP. PasskeyUpdate is #[non_exhaustive] so a future variant (e.g. a CounterRegression alarm if danger-credential-internals is ever adopted) is additive.")
@yah:assumes("The round-trip + apply tests accept EITHER PasskeyUpdate::Updated or ::Unchanged from a SoftPasskey authentication — the soft authenticator's counter behaviour is an implementation detail not worth pinning. The test asserts the answering credential is correctly identified (cred_id match), which is the contract that matters. A test that pinned Updated-vs-Unchanged would be asserting webauthn-authenticator-rs internals, not cheers behaviour.")
@yah:verify("cd external/cheers && cargo test -p cheers --features passkey")
@yah:verify("cd external/cheers && cargo test -p cheers --features passkey --doc")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
-->

<!--
@yah:ticket(R014-T1, "passkey/register.rs — start_registration → finish_registration")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:21:45Z)
@yah:status(review)
@yah:phase(P7)
@yah:parent(R014)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("PasskeyRelyingParty (P7 server-side WebAuthn) landed at crates/cheers/src/passkey/mod.rs + register.rs, gated #[cfg(feature='passkey')] in lib.rs. mod.rs: PasskeyRelyingParty wraps webauthn_rs::Webauthn — constructors new(rp_id, rp_origin: Url) and builder(rp_id, rp_origin) -> PasskeyRelyingPartyBuilder (rp_name / allow_subdomains / append_allowed_origin / timeout, then .build()); accessors rp_id/rp_origin/rp_name/allowed_origins/webauthn(). PasskeyError #[non_exhaustive]: Config(#[source] WebauthnError) [deploy-time config bug], Ceremony(#[source] WebauthnError) [per-request verify failure], Serialize/Deserialize(serde_json::Error), WrongBinding{found: DeviceBinding}. Curated re-exports of the webauthn-rs protocol types consumers need (CreationChallengeResponse, RegisterPublicKeyCredential, Passkey, PasskeyRegistration, PublicKeyCredential, RequestChallengeResponse, CredentialID, Url, Uuid) so products don't take a direct webauthn-rs dep. register.rs: impl PasskeyRelyingParty { start_registration(user_handle: Uuid, user_name, user_display_name, exclude: &[Passkey]) -> (CreationChallengeResponse, PasskeyRegistration); finish_registration(&RegisterPublicKeyCredential, &PasskeyRegistration) -> Passkey } — start maps the exclude Passkeys to Vec<CredentialID> for multi-credential dedup; webauthn-rs's start_passkey_registration hardcodes require_resident_key(false) so the profile is NON-discoverable by design (= plan's 'discoverable off'). Bridge free fns passkey_to_credential(UserId, DeviceId, &Passkey) -> Credential (binding=Passkey, material=serde_json passkey) and passkey_from_credential(&Credential) -> Passkey bridge P7 ceremonies to P8 CredentialStore / P12 product tables. Cargo: passkey feature = [dep:webauthn-rs, dep:thiserror, dep:serde_json]; webauthn-rs 0.5.5 default-features=false + danger-allow-state-serialisation (the start/finish state MUST be stashed server-side, single-use, confidential — documented like the OIDC flow store). Dev-dep webauthn-authenticator-rs 0.5 (softpasskey only) drives real ceremonies end-to-end (shares webauthn-rs-core, no shipped code). deny.toml: openssl/openssl-sys carve-out (see assumes); native-tls stays banned. Tests (9 unit + 1 doctest, network-free): CCR wire-shape (rp.id, user name/displayName/id, challenge non-empty, pubKeyCredParams non-empty, attestation=none, requireResidentKey=false, userVerification=required); excludeCredentials lists a supplied passkey's cred_id; full SoftPasskey register round-trip yields a Passkey; finish rejects a credential bound to a different challenge (state binds the challenge); bridge round-trips a passkey through a Credential; bridge rejects non-passkey binding + garbage material; builder configures name + extra origin; new() rejects an origin unrelated to rp_id. cargo test -p cheers --features passkey: 128 unit + 10 doctests pass. cargo test --workspace --all-features, cargo check --workspace --all-features, cargo deny --all-features check: all clean. Passkey code is clippy-clean (2 PRE-EXISTING clippy warnings remain in email/template.rs (P3) + providers/apple/redirect.rs (P6), unrelated to P7 — see cleanup).")
@yah:next("R014-T1 ready for human sign-off. R014-T2 picks up next: passkey/authenticate.rs — start_authentication -> finish_authentication. Shape mirrors register: impl PasskeyRelyingParty { start_authentication(allow_credentials: &[Passkey]) -> (RequestChallengeResponse, PasskeyAuthentication) [maps to webauthn-rs start_passkey_authentication(&[Passkey])]; finish_authentication(&PublicKeyCredential, &PasskeyAuthentication) -> AuthenticationResult }. Caller loads the user's stored Passkeys (via passkey_from_credential) and passes them as the candidate set; finish returns an AuthenticationResult whose cred_id() identifies which credential answered and whose needs_update()/counter feed Passkey::update_credential(&result) for clone/counter-regression detection — surface that as a helper or document it. Reuse the SoftPasskey dev-dep: WebauthnAuthenticator::new(SoftPasskey::new(true)).do_authentication(origin, rcr) -> PublicKeyCredential for the round-trip test; one SoftPasskey instance must perform BOTH register and auth (it stores the key by rp_id+cred_id), so the auth test registers first then authenticates with the same authenticator. RequestChallengeResponse/PasskeyAuthentication/PublicKeyCredential are already re-exported; add AuthenticationResult to the re-export list. Once T1+T2 are both signed off + archived, archive R014 to close P7.")
@yah:assumes("openssl/openssl-sys carve-out in deny.toml is USER-APPROVED (2026-05-26): webauthn-rs-core -> webauthn-attestation-ca hard-depends on openssl for COSE-key + X.509 attestation crypto (NOT TLS), and no pure-Rust server-side WebAuthn RP is mature enough to carry security-critical attestation crypto. openssl is only reachable via the passkey feature; the default bundle + native-client features (P8/P9) stay rustls-only. native-tls stays banned. Re-ban openssl if a future webauthn-rs drops it or passkey-auth-rs matures (see cleanup).")
@yah:assumes("Discoverable-credential registration is intentionally NOT built: webauthn-rs 0.5's start_passkey_registration hardcodes require_resident_key(false), giving the non-discoverable (server-knows-the-user) Passkey profile the design doc specifies as default ('discoverable off'). Resident-key / username-less login would route through webauthn-rs's resident-key entry points, not the Passkey profile — a deliberate future extension, not a config no-op.")
@yah:assumes("WebAuthn user handles are Uuid (webauthn-rs's type); cheers UserId is an arbitrary String. The ceremony API takes the Uuid handle directly — the PRODUCT owns the UserId<->Uuid mapping (store the pair, or derive a stable v5 UUID). A future cheers helper user_handle_from_id(&UserId)->Uuid (UUIDv5 over a cheers namespace) would need uuid's v5 feature as a direct dep; deferred until a product asks.")
@yah:assumes("PasskeyError::Config/Ceremony keep webauthn_rs::WebauthnError as #[source], coupling cheers's public error to webauthn-rs's error type. Acceptable because the passkey feature is inherently webauthn-rs-coupled and the source chain is valuable for tracing; a webauthn-rs major bump could touch this variant's source type.")
@yah:cleanup("Long-term: track https://github.com/marirs/passkey-auth-rs — a pure-Rust, no-openssl server-side WebAuthn RP. One-maintainer repo, so adoption likely means forking or co-maintaining (per the operator). If it matures (or webauthn-rs drops openssl), migrate PasskeyRelyingParty onto it and re-ban openssl in deny.toml. The PasskeyError + Credential-bridge surface is designed to outlive the underlying crate.")
@yah:cleanup("2 pre-existing clippy warnings unrelated to P7: email/template.rs:127 (unnecessary closure for Option::None default) and providers/apple/redirect.rs:581 (doc_lazy_continuation). Fold into the owning P3/P6 tickets or a lint-sweep ticket; not touched here to keep the P7 diff focused.")
@yah:cleanup("Discoverable-credential (resident-key) registration as a future extension — see assumes. When a product needs username-less login, add a discoverable flag on the builder (default off) routing through webauthn-rs's resident-key registration methods.")
@yah:verify("cd external/cheers && cargo test -p cheers --features passkey")
@yah:verify("cd external/cheers && cargo test -p cheers --features passkey --doc")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
-->

<!--
@yah:ticket(R013-T4, "apple/jwks_cache.rs — weekly refresh + on-failure refresh")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:21:45Z)
@yah:status(review)
@yah:phase(P6)
@yah:parent(R013)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("AppleJwksCache landed at crates/cheers/src/providers/apple/jwks_cache.rs and is re-exported via providers/apple/mod.rs under #[cfg(feature='apple')]. Trait + impls: pub trait JwksFetcher: Send + Sync with async fetch(&self, &reqwest::Client) -> Result<CoreJsonWebKeySet, JwksError>. pub struct HttpJwksFetcher holds a JsonWebKeySetUrl; HttpJwksFetcher::for_apple() bakes in APPLE_JWKS_URI ('https://appleid.apple.com/auth/keys'); HttpJwksFetcher::with_url(impl Into<String>) for self-hosted or test endpoints (returns JwksError::InvalidUrl on parse fail). HttpJwksFetcher::fetch does the canonical reqwest GET → error_for_status → bytes → serde_json::from_slice::<CoreJsonWebKeySet>; failures bucket into JwksError::{Http, Parse}. pub struct AppleJwksCache<F: JwksFetcher = HttpJwksFetcher> stores (fetcher, refresh_after_seconds, Mutex<Option<CachedJwks>>) where CachedJwks holds Arc<CoreJsonWebKeySet> + fetched_at. AppleJwksCache::for_apple() shortcuts to AppleJwksCache::new(HttpJwksFetcher::for_apple()). Builders: with_refresh_after_seconds(seconds) panics on non-positive (caller bug, not runtime input). Accessors refresh_after_seconds/fetcher/cached_fetched_at/is_cached. jwks(now, &http) is the hot path: fast-path Mutex read returns Arc::clone of the cached JWKS if `now - fetched_at < refresh_after_seconds`; else releases the lock, fetches without contention, reacquires, double-checks (another task may have refreshed while we awaited), stores fresh as Arc, returns. invalidate() drops the cached slot — wire on `kid not found` from id_token verification (Apple rotated between scheduled refreshes). Concurrency: std::sync::Mutex held only across the cached-state read/write, never across the network fetch — cold-cache concurrent verifies can race into two fetches, but both succeed against the same Apple JWKS and one Arc replaces the other harmlessly. Coalescing into a single in-flight fetch (tokio OnceCell / Notify) would add complexity for a once-per-week event. Constant DEFAULT_REFRESH_AFTER_SECONDS = 604_800 (1 week) — Apple rotates on the order of months; weekly poll catches a rotation within ≤7d even between on-demand verifies. Errors (JwksError #[non_exhaustive]): Http / Parse / InvalidUrl. Failed fetches do NOT poison the cache — the next call retries from scratch (proven by fetch_failure_does_not_poison_cache test against a FailingFetcher counter). Debug shows refresh_after_seconds + cached_fetched_at. Tests (13 unit + 1 doctest, all network-free): cold cache fetch + populate; cache-hit inside window doesn't refetch; boundary `now == fetched_at + ttl` refetches (matches AppleClientSecret's `<= fresh` convention); invalidate clears + forces refetch; with_refresh_after_seconds overrides default (asserted at 60s boundary); negative/zero refresh override panics; HttpJwksFetcher::for_apple URL match; HttpJwksFetcher::with_url accepts self-hosted, rejects garbage; failed fetch leaves cache untouched and retries; Arc identity — repeated cache hits return the same Arc (ptr_eq), not a clone; Send+Sync marker. Cargo: no new deps — async-trait/openidconnect/thiserror/serde/serde_json/reqwest were already pulled by the apple feature (R013-T1+T2). cargo test -p cheers --features apple: 65 unit + 4 doctests pass. cargo test --workspace --all-features: clean. cargo check --workspace --all-features and cargo deny --all-features check: clean.")
@yah:next("R013-T4 ready for human sign-off. R013-T3 picks up next: apple/native.rs — verify_native_token against Apple JWKS. T3 consumes T4's AppleJwksCache via JwksFetcher trait (or holds an Arc<AppleJwksCache<F>> directly). Suggested shape: pub struct AppleNativeVerifier<F: JwksFetcher = HttpJwksFetcher> { client_id: ClientId, jwks_cache: Arc<AppleJwksCache<F>> } with verify_native_token(jwt: &str, expected_nonce: Option<&Nonce>, now: i64, http: &reqwest::Client) -> Result<VerifiedIdToken, AppleNativeError>. Implementation flow: cache.jwks(now, http) → IdTokenVerifier::new_public_client(client_id, IssuerUrl(APPLE_ISSUER), (*jwks).clone()) → CoreIdToken::from_str(jwt) → claims(&verifier, &nonce_or_NoNonce). On verify failure with 'kid not found' (or 'no matching key' / 'signature invalid' if Apple JUST rotated), call cache.invalidate() and retry once — this is the on-failure-refresh path the §P6 plan calls for. Errors: AppleNativeError #[non_exhaustive] = Jwks(JwksError) / IdToken(String) / Malformed(String). Return type VerifiedIdToken matches T2's so callers map providers identically into UserStore. Once T3 lands and R013-T1..T4 are all signed off + archived, archive R013 to close P6.")
@yah:assumes("std::sync::Mutex (held only across non-async sections) is the right primitive here vs tokio::sync::RwLock. The cache's contention pattern is read-mostly with rare writes; a tokio RwLock would buy reader-concurrency at the cost of pulling tokio's `sync` feature into the apple gate (currently not enabled on the optional tokio dep). std Mutex + double-checked release/re-acquire is sufficient — every Arc::clone on cache hit is unblocked.")
@yah:assumes("Cold-cache concurrent verifies may produce up to N parallel fetches when N tasks all see a stale cache simultaneously. Production traffic shapes pin one entry quickly (the second arrival hits the freshly-cached value); the worst case is exactly N redundant fetches against Apple's CDN, no client-side state corruption. If a deployment surfaces this as a real problem (Apple has rate limits, but they're per-bundle-ID and generous), swap to a single-flight pattern — tokio's `OnceCell::get_or_try_init` is the standard tool — keeping the JwksFetcher contract identical.")
@yah:assumes("DEFAULT_REFRESH_AFTER_SECONDS = 604_800 (≈ 1 week) is what Apple's own JWKS guidance + RFC 7517 §4.5 recommend for OIDC verifiers. Apple has historically kept old keys live for months after a rotation, so a weekly refresh catches a rotation while remaining easy on Apple's rate limits. Tunable via with_refresh_after_seconds for Apple-compatible OPs that rotate more aggressively.")
@yah:assumes("HttpJwksFetcher::fetch uses reqwest's `error_for_status()` before reading bytes — 4xx/5xx responses bubble up as JwksError::Http rather than producing a successful empty/error-shaped JSON parse. If Apple ever returns 200 with a JSON error envelope (they don't today), the parse path would surface JwksError::Parse, which is the correct failure mode.")
@yah:cleanup("If a future feature (auth-server side, e.g. R018) needs a generic JwksCache parameterized by issuer + URL rather than Apple-specific, move AppleJwksCache → JwksCache, drop the `Apple` prefix in jwks_cache.rs, keep `for_apple()` as the Apple-pinned constructor. The current Apple-only naming reflects that no other provider in cheers currently needs cache-with-refresh-on-failure (Google uses openidconnect's internal JWKS handling via id_token_verifier()).")
@yah:cleanup("The HttpJwksFetcher path isn't directly exercised by a wiremock test in T4 (the network-free path through CountingFetcher proves the cache logic). The redirect.rs wiremock'd round-trip *does* exercise the openidconnect-internal JWKS fetch via discover_async which uses the same reqwest GET shape — so the HTTP code path is implicitly covered. If T3's wiremock test ends up not using AppleJwksCache, add a dedicated wiremock test for HttpJwksFetcher (serve a fixed CoreJsonWebKeySet at /auth/keys + verify the cache populates correctly).")
@yah:cleanup("The CountingFetcher / FailingFetcher test fixtures are bespoke per-test types here. If R013-T3's tests also want a counting/failing JwksFetcher, lift them into a `#[cfg(test)] pub(crate) mod test_support` under apple/ — same recommendation R013-T2's cleanup note made for the IdP-fixture helpers in google.rs vs apple/redirect.rs tests. Defer until T3 actually needs them; lifting prematurely tends to fossilize the wrong shape.")
@yah:verify("cd external/cheers && cargo test -p cheers --features apple")
@yah:verify("cd external/cheers && cargo test -p cheers --features apple --doc")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
-->

<!--
@yah:ticket(R013-T3, "apple/native.rs — verify_native_token against Apple JWKS")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:21:45Z)
@yah:status(review)
@yah:phase(P6)
@yah:parent(R013)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("AppleNativeVerifier landed at crates/cheers/src/providers/apple/native.rs and is re-exported via providers/apple/mod.rs under #[cfg(feature='apple')]. Surface: pub struct AppleNativeVerifier<F: JwksFetcher = HttpJwksFetcher> { client_id, issuer, jwks_cache: Arc<AppleJwksCache<F>> }. Constructors: AppleNativeVerifier::new(client_id, Arc<AppleJwksCache<HttpJwksFetcher>>) bakes in APPLE_ISSUER; AppleNativeVerifier::with_issuer(client_id, issuer, Arc<AppleJwksCache<F>>) takes a custom issuer for tests + self-hosted Apple-compatible OPs (returns AppleNativeError::InvalidIssuer on parse fail). Accessors client_id/issuer/jwks_cache; Debug shows client_id+issuer (cache state stays opaque). verify_native_token(jwt, expected_nonce: Option<&Nonce>, now, &reqwest::Client) is the single verify entry point: pulls the cached JWKS via jwks_cache.jwks(now, http), CoreIdToken::from_str(jwt) → AppleNativeError::Malformed on parse fail, builds CoreIdTokenVerifier::new_public_client(client_id, issuer, (*jwks).clone()), calls id_token.claims with either Some(&nonce) as a strict NonceVerifier or a no-op closure (|_n: Option<&Nonce>| Ok::<_, String>(())) when expected_nonce is None — None means 'skip nonce check' (only safe when the calling product has no per-request nonce; the native iOS/macOS SDK flow normally always supplies one). Returns the same VerifiedIdToken shape as redirect.rs's AppleVerified.id_token so callers map providers identically into UserStore. On-failure JWKS refresh: if verify_once returns AppleNativeError::IdToken AND the error message looks like a JWKS rotation event (heuristic looks_like_rotation_failure — matches lower-case 'kid' / 'no matching' / 'key' / 'signature'), invalidate the cache and retry exactly once with a freshly-fetched JWKS. Claim-shape failures (wrong iss/aud, expired, nonce mismatch) do NOT trigger the retry — they're deterministic regardless of which JWKS is cached. The rotation retry path is the §P6 plan's 'on-verify-failure refresh' requirement. Errors (AppleNativeError #[non_exhaustive]): Jwks(JwksError, #[from]) / Malformed(String) / IdToken(String) / InvalidIssuer(String). Tests (16 unit + 1 doctest, all network-free): happy path (claims match, exactly one fetch); explicit nonce-skip path; wrong-audience reject WITHOUT triggering rotation retry (asserted via fetcher call count = 1); wrong-issuer reject; nonce mismatch reject; expired token reject; malformed JWT reject (Malformed not IdToken); the actual rotation-retry path — token signed kid=B, first JWKS only has kid=A → verify fails → invalidate + refetch → second JWKS has kid=B → verify succeeds (fetcher called exactly 2 times); only-retries-once path — both JWKSes missing the right kid → verify fails after exactly 2 fetches, no infinite spin; classifier unit tests pinning the rotation-shape heuristic + the claim-shape exclusions; with_issuer rejects garbage URL; new() pins APPLE_ISSUER; Send+Sync marker. The rotation test infrastructure uses a ScriptedFetcher (StdMutex<Vec<CoreJsonWebKeySet>>) that pops one canned response per call — same shared RSA test PEM as google.rs / apple/redirect.rs / apple/native.rs to keep test plumbing minimal, just different kid values per JWK. cargo test -p cheers --features apple: 79 unit + 5 doctests pass. cargo test --workspace --all-features: clean. cargo check --workspace --all-features and cargo deny --all-features check: clean (no new exceptions/ignores).")
@yah:next("R013-T3 ready for human sign-off. R013 (P6 Apple Sign In) is now feature-complete pending sign-off of T1+T2+T3+T4. Once all four T-subs are signed off and archived, archive R013 to close P6 — that unblocks Q003's downstream relays (R014 P7 passkey is the next provider on Q003) and gets the cheers-providers quest closer to ready for yah-platform's R018-T2 OIDC routes (P12) to consume Apple in production. For yah-platform integration: the typical wiring is one Arc<AppleJwksCache> shared across both AppleRedirectProvider (T2 — web flow) and AppleNativeVerifier (T3 — native iOS/macOS flow), so the JWKS fetch cost is amortized across both paths. Biggest gotcha for downstream wiring: SameSite=None on the bound-state cookie for the form-post callback (cross-site POST).")
@yah:assumes("The looks_like_rotation_failure heuristic deliberately over-matches — matching 'key' / 'signature' / 'kid' / 'no matching' (case-insensitive) in the verifier error message. A false positive costs at most one extra HTTP GET against /auth/keys; a false negative costs failing to recover from a rotation until the next weekly refresh tick. If openidconnect changes its error strings, the test rotation_classifier_matches_jwks_shapes will flag a regression — re-evaluate the substring list at that point.")
@yah:assumes("CoreIdToken::from_str parses the compact JWT form (header.payload.signature, b64url, dots) directly — distinct from openidconnect's serde path which expects a JSON-quoted JWT string. The Malformed error path catches early decode failures (not three chunks, base64 invalid, JSON body invalid); IdToken catches post-decode failures (signature/iss/aud/exp/nonce mismatches).")
@yah:assumes("AppleNativeVerifier holds Arc<AppleJwksCache<F>> rather than owning the cache — by design, so a process can share one cache across redirect (T2) + native (T3) and any future Apple-server-side verification. The Arc<> wrapping is a contract: callers wire one cache once at boot and share. If a deployment for whatever reason wants per-verifier caches, just construct independent Arc<AppleJwksCache>s; the contract still holds.")
@yah:assumes("Tests use Utc::now() for token iat/exp so openidconnect's system-clock-based exp/nbf checks pass without time-injection. cheers doesn't expose a clock-injection knob on AppleNativeVerifier because pulling chrono into the runtime API surface only to satisfy that one openidconnect setter would be a heavy dep for a feature that's only useful in tests. If a future deployment ever needs to verify tokens against an injected clock (e.g. for replay-from-archive analysis), the natural seam is a with_time_fn(impl Fn() -> i64) builder that internally converts to DateTime<Utc> — would require chrono as a runtime dep at that point.")
@yah:cleanup("The TEST_RSA_PEM constant is now duplicated across three modules: google.rs, apple/redirect.rs, and apple/native.rs (same openidconnect-test PEM each time). When the dedup R013-T2 cleanup note flagged actually lands (move IdP-fixture helpers into crates/cheers/tests/support/ or a #[cfg(test)] pub(crate) module), TEST_RSA_PEM is the obvious first thing to share. Three call sites is the tipping point R013-T2's cleanup noted.")
@yah:cleanup("ScriptedFetcher in native.rs::tests is a one-off — not the same shape as CountingFetcher/FailingFetcher in jwks_cache.rs::tests. If R018's yah-platform-side integration tests want similar scripted responses, this is a candidate for the same shared-test-support module. Defer.")
@yah:cleanup("verify_native_token always retries on rotation-shaped errors — but the retry uses the SAME `now` as the first call. That's correct (the JWT's exp doesn't change between attempts), but if a deployment ever wants to inject clock-skew tolerance specifically for the retry path (e.g. 'accept tokens that were valid 5s ago in case the cache fetched while iat ticked forward'), that'd be a future builder on AppleNativeVerifier. Defer until a real symptom shows up.")
@yah:verify("cd external/cheers && cargo test -p cheers --features apple")
@yah:verify("cd external/cheers && cargo test -p cheers --features apple --doc")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
-->

<!--
@yah:ticket(R013-T2, "apple/redirect.rs — form-post callback + one-shot name capture")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:21:45Z)
@yah:status(review)
@yah:phase(P6)
@yah:parent(R013)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("AppleRedirectProvider landed at crates/cheers/src/providers/apple/redirect.rs and is re-exported via providers/apple/mod.rs under #[cfg(feature='apple')]. Surface: pub struct AppleRedirectProvider<S> { metadata: CoreProviderMetadata, client_id, redirect_uri, secret_gen: Arc<AppleClientSecret>, scopes, flow_ttl_seconds, flows }. Constructors: AppleRedirectProvider::from_provider_metadata(metadata, client_id, redirect_uri, secret_gen, flows) for the offline path (pair with apple_provider_metadata()); AppleRedirectProvider::discover(client_id, redirect_uri, secret_gen, flows, &http) bakes in APPLE_ISSUER=https://appleid.apple.com. Builders with_scopes(...) and with_flow_ttl_seconds(...). Accessors scopes/flow_ttl_seconds/flows/secret_gen + invalidate_client_secret() pass-through. begin(now) builds a placeholder-secret CoreClient, mints PKCE+CSRF+nonce, calls authorize_url with the APPLE_DEFAULT_SCOPES=['name','email'] and add_extra_param('response_mode','form_post'), stashes an OidcFlowState (via the new OidcFlowState::from_parts ctor), returns OidcBegin{authorize_url, csrf_state}. finish_form_post(form, &http, now) atomically takes the flow → into_parts(), checks csrf+expiry, mints a fresh ES256 client_secret JWT via secret_gen.current(now), rebuilds the CoreClient with AuthType::RequestBody (the LOAD-BEARING Apple-ism — openidconnect/oauth2's default BasicAuth puts client_id:client_secret in the Authorization header which Apple rejects), exchange_code(code).set_pkce_verifier(verifier).request_async(http), verifies the id_token against the stashed nonce, packages a FirstLoginName parsed from the form's `user` field, returns AppleVerified{id_token: VerifiedIdToken, first_login: FirstLoginName}. AppleCallbackForm::parse(body) decodes application/x-www-form-urlencoded → {code, state, user_json, error} via openidconnect::url::form_urlencoded — eagerly surfaces AppleRedirectError::Provider when Apple's `error` field is set rather than continuing with incomplete state. FirstLoginName newtype wraps Option<String>: from_apple_user_field(Option<&str>) parses Apple's {name:{firstName, lastName}, email} JSON tolerantly (absent/empty/malformed → empty()), combines first+last with a space, trims whitespace per-component. Module docs spell out the five Apple-isms (form_post, one-shot user, ES256 client_secret, pairwise sub, @privaterelay.appleid.com email) and the caller-side SameSite=None cookie requirement (form_post is a cross-site POST so SameSite=Lax cookies are *not* sent). apple_provider_metadata() returns a hand-built CoreProviderMetadata pinned to APPLE_AUTHORIZATION_ENDPOINT/APPLE_TOKEN_ENDPOINT/APPLE_JWKS_URI/subject_types=Pairwise/id_token_signing_alg=RS256/EmptyAdditionalProviderMetadata for the offline-friendly path and as a fallback if Apple's discovery JSON ever stops parsing as CoreProviderMetadata. AppleRedirectError #[non_exhaustive]: Oidc(OidcError, from) / MissingFormField(&'static str) / Provider(String) / ClientSecret(ClientSecretError, from). Side touches: (1) oidc_generic::OidcFlowState gained two new pub methods — from_parts(csrf, nonce, verifier, expires_at) -> Self (matches the R012-T1 cleanup note's anticipated shape) and into_parts(self) -> (CsrfToken, Nonce, PkceCodeVerifier, i64) (openidconnect's PkceCodeVerifier is intentionally non-Clone, exchange_code takes it by value, so the apple finish path needs ownership). The Memory store path is unchanged. (2) providers/mod.rs gates `pub mod oidc_generic` under any(google, apple) so the shared core compiles under apple-only. (3) lib.rs gates `pub mod providers` under any(google, apple). (4) Cargo.toml's apple feature now pulls dep:openidconnect + dep:async-trait alongside dep:jsonwebtoken/serde/serde_json/thiserror. Tests (26 new unit + 1 doctest in redirect.rs): form parsing — happy/no-user, percent-decoded user-JSON verbatim, provider error short-circuit, missing code/state, ignore unknowns; FirstLoginName — both/first-only/last-only/whitespace-trim/absent/no-name/malformed-JSON-is-empty; hand-built metadata URL match; begin URL shape with response_mode=form_post pinned + scope set + PKCE + state + nonce; begin stashes flow keyed by csrf; finish unhappy paths (UnknownFlow on never-stashed state, FlowExpired at exact ttl boundary); three wiremock'd round-trips standing up /.well-known/openid-configuration + /auth/keys + /auth/token (RS256 id_token signed with the same openidconnect test PEM google.rs uses) — first-login name captured from the form's user field; returning-user round-trip has empty FirstLoginName; token-endpoint matcher proves the deterministic header.payload prefix of the ES256 JWT actually reaches /auth/token (the signature itself is non-deterministic — ring mixes randomization in over RFC 6979). cargo test -p cheers --features apple: 52 unit + 3 doctests pass. cargo test --workspace --all-features: clean. cargo check --workspace --all-features and cargo deny --all-features check: clean (no new exceptions/ignores).")
@yah:next("R013-T2 ready for human sign-off. R013-T3 picks up next: apple/native.rs — verify_native_token against Apple's JWKS. The iOS/macOS native flow uses ASAuthorizationController which hands back an Apple-signed id_token directly — no code exchange, no client_secret JWT, no flow store. T3 only needs to verify the id_token: signature against APPLE_JWKS_URI's RS256 keys, iss == APPLE_ISSUER, aud == ClientId, exp/nbf via the system clock (or injected `now`), nonce binding via a caller-supplied expected nonce. Suggested shape: pub struct AppleNativeVerifier { client_id, jwks_source: Box<dyn JwksSource> } with verify_native_token(jwt, expected_nonce, now) -> Result<VerifiedIdToken, AppleNativeError>. T3 returns the same VerifiedIdToken as T2's id_token field so the downstream UserStore mapping is identical. R013-T4 (apple/jwks_cache.rs) is the JwksSource impl — weekly refresh + on-verify-failure refresh, fronted by an AppleJwksCache that holds the latest JWKS plus a fetched_at timestamp. Once T3 + T4 land and R013-T1..T4 are signed off + archived, archive R013 to close P6.")
@yah:assumes("AuthType::RequestBody for Apple's /auth/token is documented in Apple's 'Generate and Validate Tokens' note — 'send credentials in the request body, not as an Authorization header.' If Apple ever accepts BasicAuth (unlikely; it'd weaken their ability to drop the request without parsing the JWT), the change here would be additive — `with_auth_type` builder or a feature flag — not breaking.")
@yah:assumes("AppleRedirectProvider rebuilds the openidconnect::CoreClient on every finish_form_post call because oauth2's CoreClient bakes ClientSecret in at construction with no setter. Reconstruction is cheap (CoreProviderMetadata clones a fixed-size struct of URLs + algorithm Vecs; no IO). If a deployment ever shows high finish-rate latency tied to this clone, swap metadata to Arc<CoreProviderMetadata> and share via Arc::clone — but defer until profiling shows the hot spot.")
@yah:assumes("FirstLoginName::from_apple_user_field is intentionally forgiving: absent/empty/malformed-JSON all yield FirstLoginName::empty() rather than an error. Apple's contract is 'may include user JSON on first auth' — not 'always includes' — so a malformed payload shouldn't block the auth flow, and a missing payload is a normal case on returning logins. The persistence contract is enforced by *naming* (FirstLoginName) + module-doc-shouting, not by a typed must-consume pattern (which would conflict with the 'absent is normal' reality).")
@yah:assumes("Default APPLE_DEFAULT_SCOPES=['name','email']: Apple drops the user JSON unless `name` is in scope. `email` is what produces the `email` id_token claim. `openid` is implicit for Apple's OIDC compliance — explicitly including it is harmless but not required.")
@yah:cleanup("The peek_stashed_nonce helper now exists in both google.rs::tests and apple/redirect.rs::tests with near-identical bodies (apple's version additionally reconstructs the PkceCodeVerifier via secret().clone() to round-trip the put). When R013-T3 lands its own wiremock'd JWKS test (or when R012's #[rstest] cleanup gets picked up), pull the IdP-fixture helpers into a `crates/cheers/tests/support/` module gated by #[cfg(any(test, feature='test-support'))]. Not worth the refactor for two cases; tipping point is three+.")
@yah:cleanup("The token_endpoint_receives_es256_client_secret_jwt test matches a header.payload prefix because ring's ECDSA mixes randomization in on top of RFC 6979 — two signatures over identical inputs differ. If a future test wants signature-equality, swap the signer impl to rust-crypto's p256::ecdsa::SigningKey (RFC 6979 strict) plus a custom jsonwebtoken signer. Worth doing only if a property test of replay-resistance ever needs deterministic JWTs.")
@yah:cleanup("AppleCallbackForm::parse ignores `id_token` form fields. Apple sends those when response_type=code id_token (implicit-hybrid flow) — cheers uses response_type=code so the field is silent. If a future native-on-web bridge wants to skip the /auth/token round-trip by using the id_token straight from the form, add AppleCallbackForm::id_token: Option<String> and an AppleRedirectProvider::verify_form_id_token(form, &http, now) that calls into R013-T3's verifier. Defer until a concrete caller asks.")
@yah:verify("cd external/cheers && cargo test -p cheers --features apple")
@yah:verify("cd external/cheers && cargo test -p cheers --features apple --doc")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
-->

<!--
@yah:ticket(R013-T1, "apple/client_secret.rs — ES256 JWT generator + cache")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:21:45Z)
@yah:status(review)
@yah:phase(P6)
@yah:parent(R013)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("AppleClientSecret landed at crates/cheers/src/providers/apple/client_secret.rs and is re-exported via providers/apple/mod.rs under #[cfg(feature='apple')]. Surface: pub struct AppleClientSecret holding (team_id, key_id, client_id, EncodingKey, ttl_seconds, refresh_margin_seconds, Mutex<Option<Cached>>). Constructor AppleClientSecret::from_p8_pem(team_id, key_id, client_id, &[u8]) parses the PEM-armored PKCS#8 P-256 .p8 Apple ships via jsonwebtoken's EncodingKey::from_ec_pem. Builders with_ttl_seconds(ttl) and with_refresh_margin_seconds(margin) validate against documented bounds (0 < ttl <= APPLE_MAX_TTL_SECONDS=15_777_000 ≈ 6 months; 0 <= margin < ttl); with_ttl_seconds also invalidates the cache so the next mint uses the new TTL. current(now) returns the cached JWT while exp - refresh_margin > now, else mints fresh — header { alg=ES256, kid=key_id, typ=JWT }, claims { iss=team_id, iat=now, exp=now+ttl, aud=APPLE_AUDIENCE='https://appleid.apple.com', sub=client_id }. invalidate() drops the cached slot — wire into the Apple /auth/token failure path so a stale key rotation can't keep serving a dead JWT. cache_expires_at() exposes the cached exp for observability/tests. Send+Sync (asserted by a marker test). Debug redacts the EncodingKey so accidental logs don't leak the private key. Constants exposed: APPLE_AUDIENCE, APPLE_MAX_TTL_SECONDS, DEFAULT_TOKEN_TTL_SECONDS=3600 (1h — cheap resign, narrow blast radius), DEFAULT_REFRESH_MARGIN_SECONDS=300 (5min per the §P6 plan). Errors (ClientSecretError #[non_exhaustive]): InvalidKey / TtlOutOfRange / InvalidRefreshMargin / Sign. Cargo: new feature 'apple = [dep:jsonwebtoken, dep:serde, dep:serde_json, dep:thiserror]'. jsonwebtoken 9 default features pull ring (already in the tree via openidconnect+reqwest) + pem + simple_asn1; no banned deps. Dev-dep p256 0.13 generates a deterministic test keypair (scalar = 31×0x00 ++ 0x42) so test JWTs decode against a stable SPKI public PEM with no OS RNG involvement. lib.rs widened to #[cfg(any(feature='google', feature='apple'))] pub mod providers so the providers/ tree compiles under apple-only. providers/mod.rs gates #[cfg(feature='apple')] pub mod apple. 17 unit tests + 1 doctest: garbage-key reject; .p8 happy path; defaults; with_ttl out-of-range (0/neg/over-max); with_ttl accepts 1 + APPLE_MAX_TTL_SECONDS (resetting margin to 0 first when needed); with_ttl rejects when default margin would consume the new window; with_refresh_margin out-of-range (neg, == ttl); header shape (alg/kid/typ); claim shape (iss/aud/sub/iat/exp) via jsonwebtoken decode under SPKI public key; wrong-key signature reject; cache reuse inside window; re-mint at the refresh-margin edge; serve cache at margin-1 second; invalidate forces re-mint; with_ttl resets cache; Debug redacts encoding_key; Send+Sync. cargo test -p cheers --features apple: 17 unit + 1 doctest pass. cargo test --workspace --all-features: clean. cargo check --workspace --all-features and cargo deny --all-features check: clean (no new license exceptions, no new advisory ignores).")
@yah:next("R013-T1 ready for human sign-off. R013-T2 picks up next: apple/redirect.rs — form-post callback + one-shot name capture. T2 layers on OidcProvider (R012-T1) with Apple's quirks: response_mode=form_post (callback POSTs body, not GETs); the 'user' JSON arrives only on the first auth response (FirstLoginName newtype per §P6 plan) and is gone forever after, so persistence on first sight is the contract; Apple's discovery sometimes omits standard fields so verify whether https://appleid.apple.com/.well-known/openid-configuration still parses as CoreProviderMetadata — if not, T2 needs a hand-built metadata fixture rather than discover_async. T2 will pass AppleClientSecret::current(now) as the openidconnect ClientSecret on each /auth/token call (refresh handled here, T2 just reads). After T2: R013-T3 (apple/native.rs — verify_native_token against Apple JWKS) and R013-T4 (apple/jwks_cache.rs — weekly refresh + on-failure refresh). Once all four T-subs sign off, archive each then R013 to close P6.")
@yah:assumes("jsonwebtoken 9's EncodingKey::from_ec_pem accepts both PKCS#8 and SEC1 PEM. Apple's .p8 download is PKCS#8 PEM — verified by the from_p8_pem_accepts_pkcs8_p256 test which constructs a real PKCS#8 PEM via p256::SecretKey::to_pkcs8_pem and feeds it through. If Apple ever switches wrapping formats, the parser already handles SEC1; if they go off-format entirely (DER, JWK), this constructor needs a parallel from_p8_jwk / from_p8_der.")
@yah:assumes("DEFAULT_TOKEN_TTL_SECONDS=3600 (1h) is a deliberate trade-off — Apple permits up to APPLE_MAX_TTL_SECONDS (~6 months) but signing is microseconds and a short window narrows the blast radius if a JWT ever leaks. Callers running on tight per-request budgets can opt in via with_ttl_seconds(APPLE_MAX_TTL_SECONDS); the cache shape doesn't change. Re-evaluate if any prod deployment surfaces hot-path mint latency in tracing.")
@yah:assumes("Mutex<Option<Cached>> is fine for the expected contention pattern — one mint per ~ttl seconds with a fast cached-read path. If a deployment ever sees true high-contention mint storms (many threads passing the refresh threshold concurrently), the right swap is ArcSwap or a thundering-herd-aware once-cell. Surface stays. Until then, std::sync::Mutex avoids a tokio runtime requirement on the synchronous current() path (P6's downstream T2 redirect handler is async, but the cache itself doesn't need to be).")
@yah:assumes("The 'wrong-key signature is rejected' test relies on jsonwebtoken's error Display containing the substring 'signature' (case-insensitive). Tightening to a specific ErrorKind would require pulling jsonwebtoken::errors::ErrorKind into the test surface — left fuzzy for now since the only purpose is to prove a different keypair doesn't verify. Worth tightening when R013-T3 lands and starts exercising the same library for JWKS-side verification.")
@yah:cleanup("AppleClientSecret holds exactly one (team_id, key_id, client_id) tuple — the cache is a single slot. If a future deployment wants to serve multiple client_ids off one private key (Apple permits this for app-group setups: Services ID + the bundle IDs it covers), the cache becomes a HashMap<client_id, Cached>. Defer until a concrete need shows up — the common case is one Services ID per binary.")
@yah:cleanup("Dev-dep p256 0.13 exists solely to mint a deterministic test keypair. If a future T-sub (most likely T3 — Apple JWKS verification) needs to mint test EC signatures too, refactor to a shared test-support module in crates/cheers/tests/support/ rather than duplicating fixed_keypair() in each leaf's #[cfg(test)] block.")
@yah:verify("cd external/cheers && cargo test -p cheers --features apple")
@yah:verify("cd external/cheers && cargo test -p cheers --features apple --doc")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
-->

<!--
@yah:ticket(R012-T2, "google.rs — GoogleProvider newtype + wiremock tests")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:21:45Z)
@yah:status(review)
@yah:phase(P5)
@yah:parent(R012)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("GoogleProvider landed at crates/cheers/src/providers/google.rs and is re-exported via providers/mod.rs under #[cfg(feature='google')]. Surface: pub struct GoogleProvider<S>(OidcProvider<S>) with Deref<Target=OidcProvider<S>> so begin/finish/with_scopes/with_flow_ttl_seconds/scopes/flow_ttl_seconds/flows pass through transparently — the newtype only exists to bake in pub const GOOGLE_ISSUER='https://accounts.google.com'. Constructors: GoogleProvider::discover(client_id, client_secret, redirect_uri, flows, &reqwest::Client) wraps OidcProvider::discover with the Google issuer hard-coded; GoogleProvider::from_provider_metadata(metadata, ...) delegates straight through for pre-cached metadata or tests; into_inner() returns the wrapped OidcProvider. Side touch: OidcFlowState gained pub accessors csrf_token() / nonce() / pkce_verifier() (alongside existing expires_at/is_expired_at) — needed so any non-Memory OidcFlowStore impl can serialize the stashed flow state, and used by google.rs tests to bind a freshly-minted RS256 id_token to the in-flight nonce. The Nonce/CsrfToken/PkceCodeVerifier types still have their own redacting Debug, and the OidcFlowState Debug impl continues to redact all three. Tests (4 new): google_issuer_constant_is_the_canonical_url asserts the constant + that it parses as IssuerUrl; from_provider_metadata_derefs_to_oidc_provider drives begin() through Deref against the hand-rolled Google discovery fixture and verifies authorize_url.host_str() == 'accounts.google.com'; discover_then_finish_round_trip_extracts_claims stands up a wiremock IdP serving /.well-known/openid-configuration + /jwks + /token, runs OidcProvider::discover against it, peeks the stashed nonce, mints an RS256 id_token via openidconnect's own CoreRsaPrivateSigningKey + CoreIdToken::new (using the published TEST_RSA_PEM from openidconnect's own test fixtures, kid='cheers-test-key'), wraps the result in GoogleProvider, and verifies the full begin/finish round-trip extracts {issuer,subject,email,email_verified='alice@example.com'/true,name='Alice Anderson'}; finish_falls_back_to_localized_name_when_untagged_missing exercises the same path with a localized-only ('en' tag) name claim and asserts the un-locale-tagged fallback in extract() picks up 'Bob Localized'. Helpers: dummy_http() (no-redirect reqwest), mount_discovery_and_jwks() (the static GETs), mount_token_endpoint() (the dynamic POST after begin), peek_stashed_nonce() (take→clone→put). Note: GoogleProvider::discover itself can't be exercised against wiremock because it bakes in the Google issuer URL — tests use OidcProvider::discover with the wiremock URL and wrap in GoogleProvider, which is the same code path. Cargo: added dev-deps wiremock 0.6 and chrono 0.4 (default-features=false, features=['clock']); chrono was already a transitive of openidconnect so no new tree pulled in. cargo test -p cheers --features google: 19 unit + 2 doctests pass; cargo test --workspace --all-features: clean (64 unit + 34 integration + 12 doctests). cargo check --workspace --all-features and cargo deny --all-features check both clean.")
@yah:next("R012-T2 ready for human sign-off. Once R012-T1 + R012-T2 are both signed off and archived, archive R012 to close P5. R013 (P6 Apple) is next on Q003: layers Apple's form-post + ES256 client_secret + JWKS-cache + first-login name capture on top of OidcProvider. The cheers-plan.md gotcha note that 'Apple's discovery sometimes omits fields' is the first thing to check in R013-T1 — verify Apple's https://appleid.apple.com/.well-known/openid-configuration still parses as CoreProviderMetadata, otherwise R013 needs a hand-built metadata fixture rather than discover_async.")
@yah:assumes("The published TEST_RSA_PEM (PKCS#1, 2048-bit) shipped with openidconnect's own test fixtures is fine as a test signing key — it's deterministic, already in the workspace's transitive dep graph, and never reaches production. If a future test wants per-test key isolation (e.g. for negative tests proving signature rejection across different keys), generate a second fixture pair rather than reusing this one. Reusing the openidconnect test key is the same pattern that crate uses for its own RS256 round-trip tests.")
@yah:assumes("OidcFlowState's new public accessors (csrf_token/nonce/pkce_verifier) are read-only borrows; they don't enable arbitrary construction of OidcFlowState from outside oidc_generic.rs. A real Redis/Postgres OidcFlowStore impl will additionally need a pub constructor (probably OidcFlowState::from_parts(csrf, nonce, verifier, expires_at)) for the deserialize path — deferred until a non-memory store actually lands (likely R018-T2 or an external product wiring).")
@yah:assumes("wiremock's set_body_json sets the response Content-Type to application/json — openidconnect's discovery code refuses anything else (we hit this on the first test run with set_body_string + insert_header, which wiremock silently overrode to text/plain). Worth knowing for future tests of the discovery path.")
@yah:cleanup("GoogleProvider::discover is currently untestable against a mock because it hard-codes GOOGLE_ISSUER. If a future test wants to verify the constant flows through to the actual discover_async call (e.g. via a custom http client that intercepts and asserts on the requested URL), the cleanest seam is a pub(crate) GoogleProvider::discover_at(issuer, ...) test helper rather than parameterizing the public API. Not worth doing until we have a concrete test that needs it.")
@yah:cleanup("The two wiremock round-trip tests share a lot of setup (mount_discovery_and_jwks → discover → begin → peek_nonce → mint_id_token → mount_token_endpoint → finish). If R013 (Apple) adds a third with the same shape, fold them into a #[rstest::rstest]-driven parameterized test. Defer the macro/dep cost until at least one more case lands.")
@yah:verify("cd external/cheers && cargo test -p cheers --features google")
@yah:verify("cd external/cheers && cargo test -p cheers --features google --doc")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
-->

<!--
@yah:ticket(R012-T1, "oidc_generic.rs — OidcProvider wrapping openidconnect::Client + PKCE/state/nonce")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:21:45Z)
@yah:status(review)
@yah:phase(P5)
@yah:parent(R012)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("oidc_generic.rs landed at crates/cheers/src/providers/oidc_generic.rs, re-exported via crates/cheers/src/providers/mod.rs (#[cfg(feature='google')] in lib.rs). Surface: OidcProvider<S: OidcFlowStore> wraps openidconnect::CoreClient<EndpointSet, EndpointNotSet, EndpointNotSet, EndpointNotSet, EndpointMaybeSet, EndpointMaybeSet> (the typestate produced by from_provider_metadata + set_redirect_uri). Constructors: from_provider_metadata(metadata, client_id, client_secret, redirect_uri, flows) for in-process metadata + the testing path, and discover(issuer, client_id, client_secret, redirect_uri, flows, &reqwest::Client) for the network-fetched form (uses CoreProviderMetadata::discover_async). Builder-style with_scopes / with_flow_ttl_seconds; defaults are DEFAULT_SCOPES=['openid','email','profile'] and DEFAULT_FLOW_TTL_SECONDS=300. Flow lifecycle: begin(now) mints PKCE (PkceCodeChallenge::new_random_sha256 → S256) + CsrfToken + Nonce, threads them through CoreClient::authorize_url, stashes OidcFlowState{csrf_token,nonce,pkce_verifier,expires_at} keyed by csrf_state.secret() in the user-supplied store, and returns OidcBegin{authorize_url, csrf_state} for the redirect. finish(callback, &reqwest::Client, now) atomically takes the stashed state, checks state-match + expiry, calls exchange_code(code).set_pkce_verifier(v).request_async(http), pulls the id_token, verifies with the stashed nonce via client.id_token_verifier(), and returns VerifiedIdToken{issuer,subject,email,email_verified,name}. Store contract: OidcFlowStore (async_trait, Send+Sync) with put + take (single-use); MemoryOidcFlowStore provides a Mutex<HashMap> impl + .gc(now)/.len()/.is_empty() for tests/dev/single-replica. OidcFlowState Debug impl redacts the three secrets (csrf/nonce/pkce_verifier) so accidental logs stay safe. OidcError #[non_exhaustive]: Discovery / Config / Http / UnknownFlow / FlowExpired / StateMismatch / MissingIdToken / IdToken / Store. Cargo: new feature 'google = [\"dep:openidconnect\",\"dep:async-trait\",\"dep:thiserror\"]'. openidconnect 4.0.1, default-features=false, features=['reqwest','rustls-tls'] (matches deny.toml's openssl/native-tls ban). serde_json moved to dev-deps (needed for the discovery JSON fixture). Tests (15 unit + 1 doctest) cover: FlowStore put/take single-use, take-missing, gc-by-expiry, default scopes+TTL, with_scopes replacement, with_flow_ttl override, begin URL params (scheme/host/path + response_type=code + client_id + redirect_uri + scope list + code_challenge_method=S256 + non-empty code_challenge/state/nonce + csrf_state in URL == returned csrf_state), begin stash keying + expiry math, unique state per call, custom scopes propagate to URL, finish→UnknownFlow on missing state, finish-on-replay returns UnknownFlow (first call burns the flow even though HTTP fails), FlowExpired on stale ttl, and Debug-redacts-secrets. cargo test -p cheers --features google: 15+1 pass. cargo check --workspace --all-features: clean. cargo deny --all-features check: clean (one new advisory ignore — see assumes).")
@yah:next("R012-T2 picks this up — GoogleProvider newtype + wiremock tests. Suggested shape: pub struct GoogleProvider<S>(OidcProvider<S>); with Google::discover(client_id, client_secret, redirect_uri, flows, http) baking in IssuerUrl('https://accounts.google.com'), and Deref<Target=OidcProvider<S>> so callers reach begin/finish transparently. Tests should drive a wiremock'd Google: stand up wiremock with a fixed RSA JWK in /openid-configuration + /jwks, hand the discovery URL to OidcProvider::discover (override the issuer URL to point at wiremock — Google's iss is 'accounts.google.com' but the discovery endpoint can be mocked), mint a real RS256 id_token signed by the wiremock key, and exercise the full begin/finish round-trip. The 'name' claim extraction in extract() returns the un-locale-tagged value first then falls back to the first localized entry — worth a fixture covering both shapes. R013 (P6 Apple) then layers Apple's form-post + ES256 client_secret + first-login name capture on top of OidcProvider; the openidconnect-rs author docs note Apple's discovery sometimes omits fields, so check whether Apple's discovery still parses as CoreProviderMetadata.")
@yah:assumes("Marvin Attack (RUSTSEC-2023-0071) on the `rsa` crate was added to deny.toml's advisories.ignore. Justification (also captured inline in deny.toml): the timing sidechannel only affects RSA *private-key* operations (signing/decryption); cheers is an OIDC *relying party* and only touches RSA *public-key* verification (the IdP's JWKS). openidconnect's CoreRsaPrivateSigningKey path (the only signing surface) isn't compiled or referenced from cheers. Re-evaluate if cheers ever ships an OP/IdP role or adds RSA private-key handling for any reason.")
@yah:assumes("Caller-side session binding (cookie ↔ csrf_state) is documented as a 'Gotcha' in the module-level doc but is NOT enforced by OidcProvider. The flow store is keyed by csrf_state.secret() alone; the calling product is responsible for refusing a callback where the cookie doesn't match. R012-T2's GoogleProvider docs should reiterate; R018-T2 (yah-platform OIDC routes) is where the binding actually gets wired.")
@yah:assumes("DEFAULT_SCOPES = ['openid','email','profile'] matches what 99% of consumer OIDC products want; Google explicitly returns email + email_verified + name when those scopes are granted. If a provider needs different defaults (e.g. enterprise SSO requesting 'groups'), with_scopes(...) replaces the set rather than appending — chosen to keep the surface tiny.")
@yah:cleanup("The CoreClient typestate is locked to (EndpointSet, EndpointNotSet, EndpointNotSet, EndpointNotSet, EndpointMaybeSet, EndpointMaybeSet) because that's what from_provider_metadata produces. If a provider needs the revocation endpoint (e.g. Google's RFC 7009 revocation flow — see openidconnect's google.rs example using AdditionalProviderMetadata) we'd extend the typestate. Defer until a caller actually wants /revoke.")
@yah:cleanup("finish() takes &reqwest::Client because that's what openidconnect's AsyncHttpClient ergonomically resolves to. If a non-reqwest HTTP client ever matters, the bound can be widened to <H: openidconnect::AsyncHttpClient<'a, ...>>; left tight for now since reqwest+rustls is what the workspace uses everywhere.")
@yah:verify("cd external/cheers && cargo test -p cheers --features google")
@yah:verify("cd external/cheers && cargo test -p cheers --features google --doc")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
-->

<!--
@yah:ticket(R010-T2, "Mailer trait + LettreMailer impl + HTML/plaintext templates")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:21:45Z)
@yah:status(review)
@yah:phase(P3)
@yah:parent(R010)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("Mailer surface landed across three new modules. (1) crates/cheers/src/email/mailer.rs — async_trait Mailer { send(&EmailMessage) -> Result<(), MailerError> }; EmailMessage {to, from, reply_to, subject, text, html} #[non_exhaustive] with builder helpers (text required for deliverability, html optional triggers multipart/alternative); MailerError { Build(String) | Transport(String) } separates caller-bug from retryable; CapturingMailer (Mutex<Vec<EmailMessage>>) for tests/dev with .captured()/.last()/.len()/.clear(). (2) crates/cheers/src/email/template.rs — MagicLinkEmail builder (product_name + from required; subject/greeting/intro/button_label/fallback_intro/expiry_notice/footer all optional with sane defaults like 'Sign in to {product_name}'). render(to, &MagicLinkRequest) -> EmailMessage builds plaintext + system-ui-styled HTML body with the URL embedded once via button + once verbatim for paste-fallback. HTML body escapes user-facing copy and href attribute (defense-in-depth — PASETO v4 chars are URL-safe by construction). (3) crates/cheers/src/email/lettre_mailer.rs gated #[cfg(feature='email-lettre')] — LettreMailer wraps AsyncSmtpTransport<Tokio1Executor>; constructors LettreMailer::starttls(host, user, pw) (port 587) and LettreMailer::implicit_tls(host, user, pw) (port 465) cover the common shapes; from_transport(...) escape hatch for pooled/custom configs. send(...) parses Mailbox, builds Message via lettre's MultiPart::alternative when html is present, maps lettre errors -> MailerError::{Build,Transport}. Cargo: new sub-feature 'email-lettre = [email, dep:lettre, dep:tokio]' so trait-only consumers don't pull lettre + tokio + rustls. lettre 0.11 with default-features=false + smtp-transport + tokio1-rustls-tls + builder + hostname (rustls only — deny.toml bans openssl/native-tls). tokio 1 with rt + macros (also added to dev-deps for the #[tokio::test] in lettre_mailer.rs). deny.toml: added '0BSD' to licenses.allow for quoted_printable (transitive of lettre, OSI-approved zero-clause BSD). 22 unit + 2 doctests pass under --features email; 25 unit + 3 doctests pass under --features email-lettre (incl. integration_provider_template_capturing_mailer which is the §P3 mock-mailer-captures-URL test). cargo check --workspace --all-features + cargo deny --all-features check both clean.")
@yah:next("R010-T2 ready for human sign-off. Once R010-T1 + R010-T2 are both signed off and archived, archive R010 to close P3. Q003 (cheers providers) still has R011 (P4 email+password), R012 (P5 OIDC+Google), R013 (P6 Apple), R014 (P7 passkey) before the providers quest closes.")
@yah:assumes("LettreMailer constructors hard-code STARTTLS-on-587 and implicit-TLS-on-465 because that's what every managed SMTP provider (SES, Postmark, Mailgun, …) ships. Anything custom (port 25, no auth, pooled) goes through from_transport(AsyncSmtpTransport<...>). Worth surfacing in cheers.md if there's a deployment shape that needs first-class help.")
@yah:assumes("MagicLinkEmail's default copy ('Sign in to {product}', 'This link can be used once and expires soon.') is intentionally generic — most products will override at least subject + intro. The defaults are there so a misconfigured caller still gets a working email rather than empty strings.")
@yah:cleanup("If a product wants i18n, this template struct stops scaling — the natural next step is a Renderer trait the caller implements, with MagicLinkEmail as the default impl. Defer until a second locale shows up.")
@yah:verify("cd external/cheers && cargo test -p cheers --features email")
@yah:verify("cd external/cheers && cargo test -p cheers --features email-lettre")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
-->

<!--
@yah:ticket(R010-T1, "magic_link.rs — paseto sign/verify + URL builder + single-use enforcement")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:21:45Z)
@yah:status(review)
@yah:phase(P3)
@yah:parent(R010)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("magic_link.rs landed at crates/cheers/src/email/magic_link.rs (re-exported via crates/cheers/src/email/mod.rs, gated #[cfg(feature='email')] in lib.rs). Surface: MagicLinkClaims {email, jti, purpose, issued_at, expires_at} #[non_exhaustive] with purpose pinned to const PURPOSE_MAGIC_LINK='magic-link'; MagicLinkCodec wraps PASETO v4.local under additional-key 'cheers_magic' (distinct from cheers-core's 'cheers' session key — keys can't be confused even if reused), pasetors-level exp suppressed via allow_non_expiring so verify_at owns the clock; mint(email, now) -> (token, claims) with CSPRNG 16-byte jti b64url-encoded; verify_at rejects Malformed / SignatureMismatch / WrongPurpose / Expired. MagicLinkUrlBuilder appends ?token=… (or & if base already has a query) — PASETO v4 chars are URL-safe so no percent-encoding. UsedJtiStore async trait (try_mark_used returns true=fresh, false=replay) with MemoryUsedJtiStore (Mutex<HashMap>) for tests/dev/single-replica + .gc(now) for unbounded-growth. MagicLinkProvider<S> glues codec+urls+store: request(email, now) validates email shape (cheap '@' + '.' check, leaving RFC5322 to the mailer) then mints+builds URL; consume(token, now) verifies-then-marks so an expired token can't burn a legitimate jti. MagicLinkError #[non_exhaustive] (Codec(#[from] CodecError) / WrongPurpose{got} / AlreadyUsed / InvalidEmail / Store(String)). DEFAULT_TTL_SECONDS=900 const exposes the §P3 15-min recommendation. Deps added under [features] email = pasetors 0.7 / serde / serde_json / thiserror / async-trait / getrandom 0.3 / base64 0.22 (all optional; default features=[] keeps cheers slim). 16 unit tests + 1 doctest pass; cargo check --workspace + cargo deny check clean.")
@yah:next("R010-T1 ready for human sign-off. After approval, R010-T2 picks up: Mailer trait + LettreMailer impl + HTML/plaintext templates. T2 consumes MagicLinkRequest{url, token, claims} from this T1 — the URL is the artifact, the claims are useful for templating the recipient's email/subject. Suggest gating LettreMailer behind a cheers feature like 'email-lettre' so consumers can ship a custom Mailer without pulling lettre+SMTP transitive deps.")
@yah:assumes("§P3 plan calls for an integration test using a mock Mailer capturing the URL — that test belongs in R010-T2 once Mailer exists. T1 covers the equivalent via provider.request(...) returning the URL directly (no mailer involved).")
@yah:assumes("Single-replica MemoryUsedJtiStore is fine for dev/tests; production multi-replica needs a shared backend. The trait is the contract — Redis/PG impls plug in without touching MagicLinkProvider. Worth surfacing in cheers.md if not already noted.")
@yah:cleanup("MemoryUsedJtiStore::gc is caller-driven — no background timer. If a long-running process accumulates expired jtis at scale, wire a periodic gc tick (could live in MagicLinkProvider as an opt-in helper).")
@yah:verify("cd external/cheers && cargo test -p cheers --features email")
@yah:verify("cd external/cheers && cargo check --workspace")
-->

<!--
@yah:ticket(R007-T5, "Property tests for Codec mint/verify + tamper detection")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:20:28Z)
@yah:status(review)
@yah:phase(P1)
@yah:parent(R007)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:next("R007-T5 (and R007's whole P1 surface) ready for human sign-off. After approval, archive R007-T1..T5 then archive R007 — that closes P1 and unblocks R009 (P11 mesofact resolver swap) and R008 (P2 refresh rotation).")
@yah:handoff("Property tests landed in crates/cheers-core/tests/codec_proptests.rs as an integration test (exercises the public surface as a downstream consumer would). proptest 1 added as dev-dep. Generators: binding_strategy covers every DeviceBinding variant (incl. OidcGeneric with random issuer); claims_strategy emits arbitrary UserId/DeviceId/timestamps with TTL up to one day. Six properties (64 cases each): (1) hmac_roundtrip — verify_at(mint(c), c.issued_at) == c; (2) paseto_roundtrip — same for PasetoV4Codec; (3) hmac_tamper_rejected — flip one random bit in the minted token; verifier either rejects OR returns Ok(c') iff c' == original (covers b64-padding/alphabet flips that don't alter decoded bytes); (4) paseto_tamper_rejected — same for PASETO; (5) hmac_wrong_key_rejected — verifying with a different key always yields SignatureMismatch; (6) paseto_wrong_key_rejected — same for PASETO. Total: 25 unit + 6 proptest + 2 doc tests pass. cargo check --workspace and cargo deny check both pass.")
-->

<!--
@yah:ticket(R007-T4, "error.rs — typed error hierarchy via thiserror")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:20:28Z)
@yah:status(review)
@yah:phase(P1)
@yah:parent(R007)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:next("R007-T5 picks this up — property tests for Codec mint/verify + tamper detection. Add proptest dev-dep and write generators for arbitrary Claims; then test mint→verify roundtrip and per-bit tamper rejection for both PasetoV4Codec and HmacBlobCodec. Awaiting human sign-off on the Error shape — specifically whether CodecError/StoreError should remain as separate types alongside the umbrella Error, or fold entirely into Error variants.")
@yah:handoff("error.rs landed under crates/cheers-core/src/error.rs. Top-level Error #[non_exhaustive] enum wraps CodecError and StoreError via #[from] ( propagates from either), plus InvalidInput(String) for caller-misuse cases that no specific subsystem owns. cheers_core::Result<T> type alias. Per-module typed errors stay where they're raised — they remain the precise return type for single-subsystem functions, and Error is the umbrella for code that touches more than one. 5 unit tests + 1 doctest cover From conversions, ? propagation, Display, and Send+Sync+'static bound. Total: 25 unit + 2 doc tests pass.")
-->

<!--
@yah:ticket(R007-T3, "store.rs — UserStore + CredentialStore + RefreshStore traits")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:20:28Z)
@yah:status(review)
@yah:phase(P1)
@yah:parent(R007)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:next("R007-T4 picks this up — unify CodecError + StoreError into a single typed error hierarchy via thiserror (likely top-level cheers_core::Error enum that wraps both, with From impls preserved). Awaiting human sign-off on UserStore method shape (especially link_provider conflict semantics) + ProviderKey enum variants + RefreshTokenRecord field set.")
@yah:handoff("store.rs landed under crates/cheers-core/src/store.rs. Three async traits via async_trait (dyn-compatible): UserStore (find_by_provider, create, link_provider, list_devices, revoke_device), CredentialStore (put/get/delete by string key — for native P8 stores; takes Credential which holds opaque material Vec<u8>), RefreshStore (put/get/mark_consumed/revoke_chain over RefreshTokenRecord — replay-detection semantics land in R008/P2). Supporting types: ProviderKey #[non_exhaustive] enum (OidcGoogle/OidcApple/OidcGeneric{issuer}/Email/LanPair) — disambiguates the subject namespace; NewUser builder; RefreshTokenRecord struct with token/chain_id/parent/user_id/device_id/issued_at/expires_at/consumed/revoked. StoreError (NotFound/Conflict/Backend(String)) — local to this module; R007-T4 will unify with CodecError into one workspace error. Deps: async-trait 0.1 (main), pollster 0.4 (dev for block_on in tests). Tests: in-memory MemUserStore/MemCredentialStore/MemRefreshStore exercise link idempotency + conflict, put/get/delete, consume + revoke_chain; dyn-compat smoke test. 20 unit tests + 1 doctest pass.")
-->

<!--
@yah:ticket(R007-T2, "codec.rs — Codec trait + PasetoV4Codec + HmacBlobCodec")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:20:28Z)
@yah:status(review)
@yah:phase(P1)
@yah:parent(R007)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:next("R007-T3 picks this up — UserStore + CredentialStore + RefreshStore traits. Awaiting human sign-off on Codec shape (trait signature, the 'cheers'-additional-key PASETO layout, and the dot-separated HMAC wire format).")
@yah:handoff("codec.rs landed under crates/cheers-core/src/codec.rs. Codec trait: mint(&Claims)->String, verify_at(token,now)->Claims, default verify(token) using SystemTime. CodecError #[non_exhaustive] (Malformed/SignatureMismatch/Expired/Crypto/Serde) — pasetors::errors::Error mapped via From. PasetoV4Codec wraps a SymmetricKey<V4> (XChaCha20-Poly1305 + BLAKE2b); the full cheers Claims is stored under a single 'cheers' additional key and the PASETO-level exp is suppressed via allow_non_expiring so verify_at's now is authoritative (pasetors's built-in exp check uses wall clock and would override us in tests). HmacBlobCodec format: b64url(json(Claims)).b64url(HMAC-SHA256). Constant-time tag compare via subtle. Both impls reject Expired/SignatureMismatch/Malformed; tamper test covers payload-bit-flip. Deps added to cheers-core: pasetors 0.7, hmac 0.12, sha2 0.10, base64 0.22, subtle 2, thiserror 2, serde_json (promoted from dev to main). Doctest on the module roundtrips HmacBlobCodec end-to-end. 16 unit tests pass; 1 doctest passes; cargo deny check passes (pasetors adds MPL-2.0-free permissive transitive deps; advisories ok).")
-->

<!--
@yah:ticket(R007-T1, "claims.rs — Claims/Credential/DeviceBinding/UserId/DeviceId/User")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:20:28Z)
@yah:status(review)
@yah:phase(P1)
@yah:parent(R007)
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:next("R007-T2 consumes Claims via Codec trait — mint(&Claims) -> String, verify(&str) -> Result<Claims, _>. Awaiting human sign-off on Claims shape before T2 builds against it.")
@yah:handoff("claims.rs landed under crates/cheers-core/src/claims.rs. Public surface: UserId/DeviceId newtypes (transparent serde, Display/From), DeviceBinding non_exhaustive enum (Passkey, OidcGoogle, OidcApple, OidcGeneric{issuer}, EmailPassword, EmailMagicLink, LanPair — serialized as internally-tagged 'kind'), User with builder-style with_email/with_name, Credential (UserId+DeviceId+DeviceBinding+opaque material Vec<u8>), Claims (sub/device/binding/issued_at/expires_at as i64 unix seconds) with is_expired_at. All contract types are #[non_exhaustive] with explicit constructors. serde + serde_json (dev) added to cheers-core deps. 7 unit tests pass; doc-tests pass (empty).")
-->

<!--
@yah:relay(R018, "P12 — yah-platform integration")
@yah:at(2026-05-15T05:17:50Z)
@yah:status(open)
@yah:phase(P12)
@yah:parent(Q005)
@yah:next("Wire cheers (google,apple,passkey,email) into yah-platform with pg-backed UserStore + auth routes")
@yah:verify("cd yah-platform && cargo test")
@arch:see(.yah/docs/working/cheers-plan.md)
-->

<!--
@yah:relay(R017, "P10 — LAN-pair")
@yah:at(2026-05-15T05:17:49Z)
@yah:status(open)
@yah:phase(P10)
@yah:parent(Q004)
@yah:next("PairOffer/PairAccept over xlb-net with confirmation strategies")
@yah:verify("cargo test -p cheers --features lan-pair")
@arch:see(.yah/docs/working/cheers-plan.md)
-->

<!--
@yah:relay(R016, "P9 — native Apple passkey UI")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:17:47Z)
@yah:status(review)
@yah:phase(P9)
@yah:parent(Q004)
@yah:next("objc2-authentication-services bridge for ASAuthorizationController on macOS/iOS")
@yah:verify("cargo build -p cheers --features macos,passkey")
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("objc2-authentication-services bridge complete: ASAuthorizationController wrapped with declare_class! delegate (PasskeyDelegate_R016), thread-local callback state, native_registration_to_credential / native_assertion_to_credential bridge fns. Builds clean on macos,passkey. Example in crates/cheers/examples/native_passkey.rs shows Tauri wiring pattern.")
@yah:next("verify build on iOS cross-compile target: cargo build -p cheers --features ios,passkey --target aarch64-apple-ios")
@yah:next("exercise the live ceremony end-to-end in a Tauri app (needs entitlement + real device)")
@yah:next("consider bumping to objc2 0.6 when available to pick up MainThreadMarker + define_class! syntax (see cleanup note)")
@yah:verify("cargo build -p cheers --features macos,passkey")
@yah:verify("cargo build --example native_passkey --features macos,passkey")
@yah:cleanup("objc2-authentication-services is pinned at 0.2; re-audit on bumps (plan.md §P9 note)")
@yah:cleanup("objc2 0.6 renames declare_class! → define_class! and adds MainThreadMarker — migrate when upgrading")
@yah:handoff("iOS cross-compile fixed: `ios` feature now uses `webauthn-rs-proto` (no openssl) instead of `passkey` (which pulls openssl via webauthn-rs-core). `cargo build -p cheers --features ios --target aarch64-apple-ios` passes clean.")
@yah:handoff("macOS build unchanged: `--features macos,passkey` and `--example native_passkey --features macos,passkey` both still pass.")
@yah:handoff("plan.md §P9 verify command corrected: `--features ios` not `--features ios,passkey`.")
@yah:next("End-to-end ceremony in a Tauri app requires entitlement + real device — can't verify in CI. Exercise manually before shipping.")
@yah:next("objc2 0.6 renames declare_class! → define_class! and adds MainThreadMarker — migrate when upgrading (cleanup, not blocking).")
@yah:cleanup("objc2 0.6 define_class! migration when objc2 bumps past 0.5")
@yah:cleanup("webauthn-rs-proto pinned at 0.5 — re-audit when webauthn-rs bumps to 0.6")
-->

<!--
@yah:relay(R015, "P8 — CredentialStore impls")
@yah:at(2026-05-15T05:17:45Z)
@yah:status(open)
@yah:phase(P8)
@yah:parent(Q004)
@yah:next("KeyringStore + EncryptedFileStore + MemoryStore behind feature flags")
@yah:verify("cargo test -p cheers --features keyring")
@yah:verify("cargo test -p cheers --features headless")
@arch:see(.yah/docs/working/cheers-plan.md)
-->

<!--
@yah:relay(R014, "P7 — passkey (WebAuthn server-side)")
@yah:at(2026-05-15T05:17:41Z)
@yah:status(open)
@yah:phase(P7)
@yah:parent(Q003)
@yah:next("PasskeyRelyingParty wrapping webauthn-rs with register + authenticate ceremonies")
@yah:verify("cargo test -p cheers --features passkey")
@arch:see(.yah/docs/working/cheers-plan.md)
-->

<!--
@yah:relay(R013, "P6 — Apple Sign In")
@yah:at(2026-05-15T05:17:40Z)
@yah:status(open)
@yah:phase(P6)
@yah:parent(Q003)
@yah:next("ES256 client_secret JWT + redirect/native flows + JWKS cache + one-shot name capture")
@yah:verify("cargo test -p cheers --features apple")
@arch:see(.yah/docs/working/cheers-plan.md)
-->

<!--
@yah:relay(R012, "P5 — OIDC infra + Google")
@yah:at(2026-05-15T05:17:38Z)
@yah:status(open)
@yah:phase(P5)
@yah:parent(Q003)
@yah:next("Stand up OidcProvider + GoogleProvider with discovery, PKCE, state+nonce")
@yah:verify("cargo test -p cheers --features google")
@arch:see(.yah/docs/working/cheers-plan.md)
-->

<!--
@yah:relay(R011, "P4 — email + password")
@yah:at(2026-05-15T05:17:36Z)
@yah:status(review)
@yah:assignee(agent:claude)
@yah:phase(P4)
@yah:parent(Q003)
@yah:handoff("password.rs landed at crates/cheers/src/email/password.rs (re-exported from email/mod.rs under #[cfg(feature='password')]). Surface in four layers. (1) Argon2id hash+verify: PasswordHasher (Params-keyed) with hash(&[u8]) -> PHC string and verify(&[u8], &str) -> Result<VerifyOutcome, PasswordError>. VerifyOutcome { needs_rehash: bool } drives migration-on-login when stored params no longer match the hasher's current target. Defaults match OWASP 2026: m=19_456 KiB, t=2, p=1, output=32 bytes — exposed as DEFAULT_MEMORY_KIB / _TIME_COST / _PARALLELISM / _OUTPUT_LEN consts plus default_params() helper, and PasswordHasher::with_default_params() one-shot. Salt: 16 raw bytes via getrandom 0.3 + SaltString::encode_b64 — avoids opting into password-hash's getrandom feature for one call site. Verify uses Argon2::default() because the verifier reads algorithm+params straight from the PHC string. needs_rehash check uses Params::try_from(&PasswordHash). (2) BreachedPasswordChecker async trait — Ok(Some(count)) seen / Ok(None) clean / Err(_) backend-failed. NoopBreachedPasswordChecker (air-gapped) + MapBreachedPasswordChecker (RwLock<HashMap<String, u64>> for tests, curated deny-lists, layered front for HIBP). (3) PasswordPolicy<C: BreachedPasswordChecker> glues the two: hash_new_password runs the breach check first (breached → PasswordError::Breached{count}, never hashed/stored); verify is hash-only and explicitly does not invoke the checker (proven by policy_verify_does_not_invoke_checker test using a panicking stub). (4) HIBP HTTP client under password-hibp sub-feature in pub mod hibp: HibpClient with k-anonymity SHA-1 (sha1_hex_upper helper exposed for testing/custom clients), HIBP_DEFAULT_BASE_URL const, with_base_url for self-hosted equivalents, with_client to inject a configured reqwest::Client. Adds Add-Padding: true header so observers can't size-fingerprint queries. scan_hibp_body(body, suffix) extracted + tested independently with CRLF / case-insensitive / malformed-count fixtures so the network-touching code stays a thin shell. PasswordError #[non_exhaustive]: Hash(String) / Malformed(String) / WrongPassword (distinct so callers map to 401 without leaking argon2 internals) / Verify(String) / Breached{count: u64} / Checker(String). Cargo: new sub-features 'password = [\"email\", \"dep:argon2\"]' and 'password-hibp = [\"password\", \"dep:sha1\", \"dep:reqwest\", \"dep:tokio\"]'. argon2 = 0.5 default features. reqwest 0.12 default-features=false + rustls-tls (matches deny.toml's openssl/native-tls ban). examples/argon_gen.rs (required-features=[\"password\"]) regenerates the KNOWN_GOOD_PHC fixture so it's deterministic, not a guess. Tests: 36 unit + 3 doctests under --features email,password (incl. round-trip, unique-salt, wrong-password rejection, malformed PHC, needs_rehash on params change, known-good fixture pass + wrong-password reject against fixture, Noop+Map checker, policy paths for breached/fresh/backend-failure, verify-doesn't-call-checker). With --features email,password,password-hibp: +6 HIBP tests (sha1 vector 'password' → 5BAA61E4C9B93F3F0682250B6CF8331B7EE68FD8, prefix-split invariants, body scan with CRLF, miss case, case-insensitive match, malformed count → treat as 1). cargo check --workspace --all-features and cargo deny --all-features check both clean.")
@yah:next("R011 ready for human sign-off. After approval, archive R011 to close P4. Q003 (cheers providers) still has R012 (P5 OIDC+Google), R013 (P6 Apple), R014 (P7 passkey) before the providers quest closes. Also note R010 sub-tickets R010-T1 + R010-T2 are still in review awaiting sign-off — once those land, R010 itself can archive to close P3.")
@yah:assumes("OWASP's 2026 Argon2id recommendation (m=19_456 KiB, t=2, p=1) is the current guidance — if it shifts before P12, default_params() is the single place to update and existing hashes will rehash-on-login through VerifyOutcome::needs_rehash. The constants are pub so consumers who want to pin a different target can call PasswordHasher::new(Params::new(...)) explicitly.")
@yah:assumes("Backend-failure semantics for BreachedPasswordChecker: cheers surfaces PasswordError::Checker and does not impose fail-open vs. fail-closed — each product picks. The doc-comment on the trait calls this out; if a workspace-wide default is desired later, it can land as a config knob on PasswordPolicy without breaking the trait shape.")
@yah:assumes("HibpClient adds 'Add-Padding: true' so HIBP returns response-size-padded bodies (industry-standard defense against on-path observers correlating bucket size to specific prefix queries). Self-hosted HIBP-compatible services may ignore the header — harmless either way.")
@yah:cleanup("argon_gen example exists solely to regenerate the KNOWN_GOOD_PHC fixture if argon2's PHC formatting ever changes. It's not a usage example. If we later add real usage examples under examples/, consider moving argon_gen into tests/ as a helper or behind an env var so it doesn't show up in `cargo run --example` listings.")
@yah:cleanup("MapBreachedPasswordChecker uses RwLock<HashMap>; for very large curated deny-lists (top 100k passwords) a hashed-prefix index would scale better. Defer until a deployment with a baked-in deny-list materializes.")
@yah:verify("cd external/cheers && cargo test -p cheers --features email,password")
@yah:verify("cd external/cheers && cargo test -p cheers --features email,password,password-hibp")
@yah:verify("cd external/cheers && cargo check --workspace --all-features")
@yah:verify("cd external/cheers && cargo deny --all-features check")
@arch:see(.yah/docs/working/cheers-plan.md)
-->

<!--
@yah:relay(R010, "P3 — email magic-link provider")
@yah:at(2026-05-15T05:17:35Z)
@yah:status(open)
@yah:phase(P3)
@yah:parent(Q003)
@yah:next("Land magic_link token + Mailer trait + LettreMailer impl")
@yah:verify("cargo test -p cheers --features email")
@arch:see(.yah/docs/working/cheers-plan.md)
-->

<!--
@yah:relay(R009, "P11 — mesofact resolver swap")
@yah:assignee(agent:claude)
@yah:at(2026-05-15T05:17:27Z)
@yah:status(review)
@yah:phase(P11)
@yah:parent(Q002)
@yah:assumes("Once mesofact P9 lands a CookieSessionResolver with its HMAC placeholder, swapping it for cheers_core::Codec is purely additive — add cheers-core as a path dep, replace the codec call sites, update fixtures. Pre-launch, no backward-compat for old cookie formats should be needed (cheers-plan.md P11 notes 'probably not — pre-launch').")
@yah:verify("cd external/mesofact && cargo test")
@arch:see(.yah/docs/working/cheers-plan.md)
@yah:handoff("P11 landed & verified. mesofact's CookieSessionResolver dropped its placeholder HMAC and now verifies via cheers_core::Codec (path dep). Default codec PasetoV4Codec (v4.local); resolver is codec-agnostic (Box<dyn Codec>) via with_codec(). Arbitrary deploy secret -> SHA-256 -> 32-byte key. Claims map onto mesofact's {id,attrs} render shape (sub->id; device/binding/issued_at/expires_at fold into attrs, per the preserve-shape decision). router.rs + bin unchanged (signature-compatible). Dropped now-dead hmac/base64 deps. Updated tests/proxy.rs fixture + arch doc auth section. Verified: cargo test -p mesofact green (6 session unit + 19 proxy incl. real PASETO round-trip render + 4 worker).")
@yah:next("Coordinate with R019 (edge-verifiable auth, filed same day): when R019-F1 splits Codec into TokenMinter/TokenVerifier, mesofact's verify call site (proxy/session.rs resolve()) needs only its trait import swapped to TokenVerifier — cheap. R019-F2's asymmetric verifier drops in via CookieSessionResolver::with_codec() with zero caller churn. R019-F3's jti is additive (#[non_exhaustive]); User::from_claims can surface it under attrs later if wanted. Cookie format stays in sync as long as both sides use cheers_core::Claims.")
@yah:gotcha("Cross-camp path dep: external/mesofact now requires external/cheers checked out as a sibling to build (cheers-core = { path = \"../../../cheers/crates/cheers-core\" }). Two independent git repos; cargo doesn't care but a lone mesofact checkout won't compile.")
@yah:gotcha("Codec choice was a judgment call: picked PasetoV4Codec (cheers-recommended default, encrypted) over the like-for-like HmacBlobCodec. mesofact is the SSR origin and the worker never sees the token, so encrypted-at-origin claims fit. If review prefers cleartext HMAC parity, swap the default in CookieSessionResolver::new.")
-->

<!--
@yah:relay(R008, "P2 — refresh token rotation")
@yah:at(2026-05-15T05:17:26Z)
@yah:status(review)
@yah:assignee(agent:claude)
@yah:phase(P2)
@yah:parent(Q002)
@yah:handoff("refresh.rs landed at crates/cheers-core/src/refresh.rs and re-exported from lib.rs. Surface: RefreshToken (32-byte CSPRNG, b64url-no-pad → 43-char wire) and ChainId (16-byte → 22-char) newtypes with `generate()` constructors using getrandom 0.3 (added as direct dep — already in lockfile via pasetors/tempfile). RefreshRotator<'s, S: RefreshStore + ?Sized> { store, ttl_seconds } with three methods: mint_root(user, device, now) starts a fresh chain; rotate(presented, now) consumes the old token and issues a successor sharing chain_id with parent linking back; revoke_chain(chain_id) for logout/device-revoke. RefreshError #[non_exhaustive]: Unknown / Expired / Replay / ChainRevoked / Store(#[from]). Replay path: presenting an already-consumed token calls store.revoke_chain(chain_id) before returning Replay — every record in the chain becomes revoked. Mark-consumed-before-put ordering: if successor put fails, worst case is 'chain stuck, client re-auths' rather than two live tokens for one consume. Expired path is read-only (no consume, no revoke) — an expired client re-auths without tripping the alarm-bell replay path. Tests (10 unit, all in src/refresh.rs since they exercise both pub surface and a tiny in-module MemRefreshStore impl): generation randomness + length, happy-path mint+rotate, multi-hop chain continuity, replay revokes whole chain incl. still-fresh tip, expired rejected without consume, unknown rejected, explicit revoke blocks future rotation, revoke_chain scoped to its chain only. Total cheers-core: 35 unit + 6 proptest + 3 doctest pass; cargo check --workspace + cargo deny check clean.")
@yah:next("R008 ready for sign-off. After approval, archive R008 — that closes P2. Q002 still has R007 (P1, in review awaiting archive of its sub-tickets) and R009 (P11 mesofact resolver swap, open) before the foundation quest closes.")
@yah:assumes("RefreshTokenRecord shape from R007-T3 is final (token/chain_id/parent/user_id/device_id/issued_at/expires_at/consumed/revoked). If T4-style error unification later wants RefreshError folded into the workspace Error enum, add a variant + From impl — current shape stays local to the module like CodecError/StoreError do.")
@yah:verify("cd external/cheers && cargo test -p cheers-core refresh")
@yah:verify("cd external/cheers && cargo test -p cheers-core")
@yah:verify("cd external/cheers && cargo test -p cheers-core --doc")
@arch:see(.yah/docs/working/cheers-plan.md)
-->

<!--
@yah:relay(R007, "P1 — cheers-core foundation")
@yah:at(2026-05-15T05:17:24Z)
@yah:status(open)
@yah:phase(P1)
@yah:parent(Q002)
@yah:next("Land Claims/Codec/Store/Error contract surface in cheers-core")
@yah:verify("cd external/cheers && cargo test -p cheers-core")
@yah:verify("cd external/cheers && cargo test -p cheers-core --doc")
@arch:see(.yah/docs/working/cheers-plan.md)
-->

<!--
@yah:relay(R006, "P0 — workspace setup")
@yah:at(2026-05-15T05:17:22Z)
@yah:status(review)
@yah:assignee(agent:claude)
@yah:phase(P0)
@yah:parent(Q002)
@yah:handoff("Workspace bootstrapped. Cargo.toml workspace lists cheers-core + cheers; both crates have Cargo.toml + lib.rs (//!-only) + README. LICENSE-MIT/APACHE copied from xlb (MIT copyright reattributed to cheers). deny.toml mirrors xlb's policy with MPL-2.0 already allowed — comment notes webauthn-rs/authenticator as the MPL-2.0 deps incoming in P7. release-plz.toml orders cheers-core before cheers. .gitignore covers target/swap/.DS_Store. Feature gates declared (inert) on cheers crate: email, password, google, apple, passkey, keyring, headless, macos, ios, lan-pair. cargo check --workspace and cargo deny check both pass (deny emits only license-not-encountered warnings for currently-unused entries, expected).")
@yah:next("Sign off P0 and unblock R007 (cheers-core foundation: claims/codec/store/error).")
@yah:verify("cd external/cheers && cargo check --workspace")
@yah:verify("cd external/cheers && cargo deny check")
@arch:see(.yah/docs/working/cheers-plan.md)
-->

<!--
@yah:relay(Q005, "cheers in yah-platform")
@yah:at(2026-05-15T05:17:09Z)
@yah:kind(quest)
-->

<!--
@yah:relay(Q004, "cheers client surface")
@yah:at(2026-05-15T05:17:09Z)
@yah:kind(quest)
-->

<!--
@yah:relay(Q003, "cheers providers")
@yah:at(2026-05-15T05:17:09Z)
@yah:kind(quest)
-->

<!--
@yah:relay(Q002, "cheers foundation")
@yah:at(2026-05-15T05:17:05Z)
@yah:kind(quest)
-->

<!--
@yah:relay(Q001, "Camp setup: cheers")
@yah:at(2026-05-15T00:19:23Z)
@yah:kind(quest)
@yah:next("Re-open the camp setup wizard: yah://onboarding/rerun")
@arch:see(.yah/camp.toml)
-->

<!-- quest declarations — managed by yah board open --kind quest -->
